apiVersion: 1

datasources:
  - name: Tempo
    type: tempo
    access: proxy
    url: http://tempo:3200
    uid: tempo
    isDefault: false
    jsonData:
      httpMethod: GET
      tracesToLogsV2:
        datasourceUid: prometheus
      serviceMap:
        datasourceUid: prometheus
      nodeGraph:
        enabled: true
      search:
        hide: false
      lokiSearch:
        datasourceUid: ''

  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://otel-collector:8889
    uid: prometheus
    isDefault: true
    jsonData:
      httpMethod: GET
      timeInterval: 15s
