# Omega Research Feed — 2026-04-27

## Items Reviewed
3 items. Direct handle searches for @browomo, @zostaff, @hanakoxbt, @0xricker, @adiix_official, @data_sn13 returned no usable Twitter/X content via WebSearch (Google does not surface their recent posts). Fell back to topic-aligned arXiv discovery in domains these accounts typically share: LOB microstructure, RL execution, market-making.

---

## RL Market Making on Non-Stationary LOB Dynamics
**Source:** arXiv 2509.12456 — https://arxiv.org/abs/2509.12456
**Type:** paper
**Score:** 4/5 × 2/5 = 8/25 — Watch

**Summary:** PPO-based market-making agent trained against a simulator that reproduces stylized facts (clustered order arrivals, non-stationary spreads, stochastic order sizes). Compared to closed-form optimal MM. Inputs: full LOB state + inventory.

**Gap analysis:**
- Does Omega do this? **No.** Omega has no L2/order-book ingestion (known gap per `project_omega.md`), no RL agent, no market-making node.
- What would change: New `omega/nodes/marketmaking/` project + ingestion path for L2 books + RL training harness. Touches platform, not just signals.
- Dependencies: Streaming L2 feed (Coinbase WS works from US), simulator infra, PPO trainer (`stable-baselines3` or custom), Go-side latency budget for live use.

**Recommendation:** Deprioritized. Three concurrent infrastructure gaps (L2, RL, simulator) make this a multi-month build, and Victoria's mandate is directional alpha, not market-making. Park as a reference for any future MM project; do not add to the V49+ training queue.

---

## Microstructural Dynamics in Crypto LOB — "Better Inputs > Deeper Nets"
**Source:** arXiv 2506.05764 — https://arxiv.org/abs/2506.05764
**Type:** paper
**Score:** 4/5 × 4/5 = 16/25 — Queue

**Summary:** Benchmarks logistic regression, XGBoost, DeepLOB, Conv1D+LSTM on BTC/USDT Bybit LOB at 100 ms–multi-second horizons. Headline: with proper preprocessing (Kalman / Savitzky-Golay smoothing) and HPO, simple models match or beat deep ones. Reports binary + ternary classification across accuracy, robustness, latency.

**Gap analysis:**
- Does Omega do this? **Partial.** Omega has 18+ technical/regime signals but no LOB-derived microstructure features. The Kalman/Savitzky-Golay smoothing layer applies to existing signal_generation regardless of LOB.
- What would change:
  1. Cheap win: add Savitzky-Golay smoothing as a preprocessing option in `omega/nodes/victoria/signal_generation.py` for noisy signals (transfer_entropy, OBV).
  2. Larger win: when L2 ingestion lands, prefer XGBoost over DeepLOB — saves training cost, fits Omega's existing meta-model pattern.
- Dependencies: scipy (already a heavy extra). LOB part blocked on streaming infra.

**Recommendation:** Queue. Implement the smoothing experiment now as a V50/V51 ablation — touches `omega/nodes/victoria/signal_generation.py` only, gated by `v49_gates.py`. Add a pre-pass `_smooth_signal(series, method="savgol", window=5)` and A/B against current raw signal. The LOB-side claim ("XGBoost ≥ DeepLOB") becomes a useful prior whenever the order-book gap is closed — record it in the meta-learner notes so future experiments don't waste cycles training DeepLOB.

---

## Deep Learning for Direct VWAP Execution in Crypto
**Source:** arXiv 2502.13722 — https://arxiv.org/abs/2502.13722
**Type:** paper
**Score:** 3/5 × 3/5 = 9/25 — Watch

**Summary:** Bypasses the standard "predict volume curve → schedule" decomposition. Uses autodiff + custom VWAP-slippage loss to learn order allocation end-to-end. Beats conventional methods even with a linear backbone.

**Gap analysis:**
- Does Omega do this? **No.** Omega's `exit_controller.py` and sizing logic are conviction/Kelly-based; there is no VWAP/TWAP execution scheduler at all.
- What would change: New execution layer between strategy decision and venue order. Would sit alongside `exit_controller.py` as `execution_scheduler.py`.
- Dependencies: Need actual order placement (currently training is paper-trade), per-venue volume curves, and a slippage attribution model. Without live execution, this is purely simulation theatre.

**Recommendation:** Skip until Omega goes live on a real exchange. The training loop measures slippage as a fixed % bps assumption; sophisticated execution scheduling provides no measurable lift while slippage is a constant. Revisit when paper-trading transitions to live capital and real fills.

---

## Notes for Next Run
- Twitter handle search via Google is consistently empty. To actually monitor these accounts, the task likely needs nitter/RSS or X API access, not WebSearch. Flag for task author.
- Topic-aligned arXiv discovery worked as a fallback and produced one Queue-tier item (the smoothing/XGBoost paper).

---
*Generated by omega-twitter-feed-monitor scheduled task*
