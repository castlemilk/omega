"""V261 / V256 — on-chain flow-primary offline scorer (parallel book).

Builds the pre-registered flow-primary directional strategy over the V257 frozen
Coin Metrics community series (BTC+ETH) and scores it against the V261 falsifiers.
Never imports Victoria or funding_carry strategy code — parallel book, same rule
as ``omega/nodes/funding_carry`` (V255.C). No flags, no live-broker, no network.
"""
