# V232 — realized-vol term-structure inversion brake, measured on the V231 distribution

**Date:** 2026-06-23
**Author:** claude
**Parent:** V231 (distributional eval harness — falsifier **Branch A** fired: the
single-window crisis gate was INADEQUATE, $25,435 cross-window spread; V227's +$630
crisis win FAILED to generalize — min-Δ −$424.50, mean-Δ +$68.53, inert on 2024aug).
**Status:** planned (pre-registration)

## Why this branch (V231 result, verbatim)

V231 quantified the Goodhart: standing-main crisis PnL swings **+$15,928 (2020q1) →
−$9,508 (2024aug)** under identical code. **B0 fired Branch A** (spread $25k ≫ $2k →
instrument inadequate). V227's drawdown-skew is **not** a generalizable crisis win — it
helps 2022h1 (+$630.08, to the cent), hurts 2020q1 (−$424.50), and is fully inert on
2024aug ($0.00). Therefore V232 ships the next crisis bet **measured on the V231 crisis
distribution** (3 windows), with the acceptance bar V227 just failed.

## Hypothesis

A **realized-vol term-structure inversion additive brake** (short-window RV ≫
long-window RV → near-term vol spike) is **orthogonal** to V227's drawdown-magnitude
skew (vol-of-vol on a shorter timescale, not a drawdown level) and additive-brake-shaped
(the only crisis form that has ever worked — selection re-weight is closed, 5× refuted).
The bet: it produces a crisis Δ that is **positive on the mean AND the min** across the 3
crisis windows — i.e. it *generalizes* where V227 did not — without regressing trend/recent.

## Changes (V227 recipe — additive brake, NOT a re-weight)

- **NEW** `omega/nodes/victoria/signals/rv_term_structure.py`: stateless, per-ticker.
  short RV (3d) / long RV (14d). On inversion (short ≫ long), one-sided `[-1, 0]` brake.
  `math.fsum` on every reduction (V211/V217/V220/V221 channel discipline).
- `omega/nodes/victoria/signal_generation.py`: add in the **post-demean additive block**
  (`:1336–1409`), exactly where `crisis_skew` is added. Gated by the **proven V227
  drawdown-AND-gate** (`crisis_skew_regime_gate_enabled` pattern).
- `omega/nodes/victoria/features.py`: NEW `rv_term_brake_enabled: bool = False` (+
  threshold params), **default-inert** so the OFF arm reproduces standing-main
  byte-for-byte ($0.00 determinism gate must hold on every cell).
- **No selection/sizing/composite redesign** (continuous sizing reopens the V220/V221
  determinism channel — a 12-month item, explicitly out of scope).

## Eval (the V231 instrument is now standing)

Run `GATES=crisis bash scripts/v231_dist_grid.sh` but with arms = {brake-ON, brake-OFF}
(the OFF arm = current standing main *including* V227 skew, so the brake's Δ is measured
*on top of* the existing skew). 3 crisis windows × 2 arms × N=2 = 12 runs, ~8h.

### Falsifier (the bar V227 failed)

Ships iff, on the V231 crisis distribution: **mean-Δ (brake-ON − main) > 0 AND min-Δ > 0**
across all 3 windows, WITHOUT regressing trend OR recent mean-Δ below −$200 (a no-harm
spot-check on 1 window each is sufficient for V232; full trend/recent distribution is
V233). If it helps only one window (mean or min ≤ 0) → **window-luck, do NOT ship** —
exactly the V227 failure mode V231 exposed. Park and reassess.

### Pre-req trace (from V231 parking lot)
Before building: **one-shot trace of why the V227 gate was inert on 2024aug** (ON==OFF
byte-identical). The brake reuses that same AND-gate; if the gate never fires on the
13/13 yen-carry window, the brake will be inert there too and can't help the worst
crisis window. Decide whether the gate's regime/drawdown condition needs widening
*before* spending a version on a brake that the gate suppresses.

## Determinism pre-reqs (carry from the arc)
`check_no_wallclock.py` + `check_frozen_http_fence.py` + `assert_cell_identity.py` PASS
per cell. RV brake is OHLCV-only (snapshot-fed) → no HTTP fence needed. Grep its producer
for unsorted FP reductions before trusting $0.00; prefer `math.fsum` over `sorted()+sum`.

## Deferred (→ V233+)
- **B2** — re-measure V229's +$1,428 trend-IC on a ≥3-window trend distribution (needs a
  3rd trend window: widen 2024 post-halving, or accept 2020-Q4 11/13).
- **Recent 2025 distribution** (resolve MATIC→POL basket-identity fork first).
- **Track A** free DVOL options-skew MVP (Deribit public, $0, 2-of-3 windows) — only if
  appetite remains.

## Gate results

*(distributional — filled on grid completion)*

| Gate | mean PnL OFF (main+skew) | mean PnL ON (+brake) | mean Δ | min Δ | spread | verdict |
|------|----:|----:|----:|----:|----:|----|
| crisis | | | | | | |

## Conclusion

*(filled on grid completion)*

## Next steps

→ **V233**: depends on V232 — if the brake generalizes, stack + extend the distribution
to trend/recent; if not, the parking-lot gate-inertness finding or Track A DVOL MVP.
