# V226 — regime-gate the crisis-skew term + lower W

**Date:** 2026-06-18
**Author:** claude
**Parent:** V225 (`58bcec1`) — additive `crisis_skew` from frozen OHLCV. 6/6
hermetic + 12/12 cell-identity PASS, but the **HARMFUL/REVERT fork fired
decisively**: `skew_on_cycles` ≈ 188–200 in *every* gate → the realized
downside-semivariance term was a **near-constant bearish tilt**, not a
crisis-specific brake. It regressed all three gates vs within-grid equal-weight
(trend −$4,570, recent −$1,671, crisis −$970 *worse*). Reverted; flag default OFF,
code left dormant as a substrate for a regime-gated retry.
**Status:** COMPLETE (2026-06-18). **REVERT — forks #4 + #3 fired.** The regime
gate did *not* make the realized downside-semivariance brake conditional enough to
help: trend regressed **−$2,451** vs within-grid equal-weight (fork #4), crisis got
**−$568 worse** (the target gate — thesis refuted), and `skew_on_cycles` stayed
**91 (trend) / 113 (recent) ≫ 40** (fork #3 — the gate barely suppresses). The
realized-proxy crisis brake is now **twice-refuted** (always-on V225, gated V226).
Flags stay default OFF; code dormant. V227 = abandon the realized proxy → paid
implied-skew vendor, and forensics on the over-firing regime label.

## Hypothesis

V225's lesson is mechanistic and unambiguous: an *always-on* downside tilt is a
**directional bias**, not a crisis signal — it fires every cycle (semivariance is
non-zero in any 30-bar window) and so it just shorts the bull on trend/recent. The
no-harm-by-construction assumption ("≈0 outside crisis") was **refuted** by the
`skew_on_cycles` ≈ 200 observability.

**Bet:** the same `crisis_skew` term, **conditioned to fire only when the runtime
regime label ∈ {crisis, high_vol}** (reusing the V223 gate-harness label as an
*enable* condition) **and at lower weight (W: 0.5 → 0.2)**, improves the **crisis**
gate by > $200 **without regressing trend or recent by > $200**. The gate collapses
`skew_on_cycles` from ~200 to the genuine crisis subset; on trend/recent (mostly
`normal` regime) the term should be suppressed → `skew_on_cycles < 40` → no-harm by
*construction enforced by the gate*, not by an (refuted) magnitude assumption.

**Effect-magnitude prediction:** crisis Δ between +$200 and +$2,000 (gated subset
of cycles × W=0.2); trend/recent Δ within ±$200 (term gated off in `normal`).

## The regime-label source (the critical design question)

The skew value is computed/applied inside `signal_generation.py` (the
`SignalGenerationNode` sub-node), which runs *before* this cycle's regime is
finalised in `victoria_node._do_compute_signals` (VRP→regime mapping at
`victoria_node.py:1062`, `_VRP_REGIME_MAP = {"FEAR":"high_vol","COMPLACENCY":
"crisis", …}`). So the authoritative label `signals["_regime"]` (what
`strategy._cycle_regime_label` reads) is **not available in-cycle** to the gate.

**Decision:** thread the **prior cycle's** consolidated `signals["_regime"]` into
the signal generator (`victoria_node` sets `self._signals._cycle_regime_label`
each cycle from `self._last_signals["_regime"]`). This is the same VRP-mapped
{crisis, high_vol, normal, bull, bear, sideways, default} label space the strategy
acts on, with a 1-cycle lag. The lag is immaterial for the experiment: regimes
persist for many consecutive cycles (a crisis snapshot is crisis for long stretches),
so only the single transition cycle is mislabeled. This is exactly the channel
fork #3 below tests — if the label distribution is wrong, the grid surfaces it.

## Changes

- Files touched:
  - `omega/nodes/victoria/features.py` — new flag `crisis_skew_regime_gate_enabled:
    bool = False` (default OFF ⇒ V225 always-on path stays byte-reachable).
  - `omega/nodes/victoria/signal_generation.py` — `_regime_gated_skew()` pure helper
    (zeroes the term outside {crisis,high_vol} when the gate flag is ON); wired at
    the value-calc path (`:1076`); `_SKEW_W_GATED = 0.2` chosen at the application
    site when the gate is ON (V225's `_SKEW_W = 0.5` path stays reachable when OFF);
    per-cycle gate-decision log + gate counters in `_CRISIS_SKEW_STATE`;
    `self._cycle_regime_label` attribute.
  - `omega/nodes/victoria/victoria_node.py` — inject prior-cycle `_regime` into the
    signal generator each cycle.
  - `scripts/run_training.py` — observability block fields
    (`crisis_skew_regime_gate_enabled`, `skew_gate_accept_cycles`,
    `skew_gate_skip_cycles`) + banner line `crisis_skew: ON (regime_gated=1, W=0.2)`.
  - `scripts/assert_cell_identity.py` — `--expect-gate {on,off}`: when the regime
    gate is ON, the `skew_on_cycles > 0` inertness requirement is relaxed (a gated
    term legitimately fires 0 cycles on trend/recent) and instead the gate is
    asserted *evaluated* (`accept + skip cycles > 0`) — preserving the V224
    mislabeled-control and V148–V202 silent-inertness protections.
  - `tests/test_crisis_skew_regime_gate.py` — new unit tests for the gate helper.
  - `scripts/v226_smoke.sh`, `scripts/v226_run_grid.sh` — harness.
- Feature flags toggled (V226 grid skew-ON cells): `crisis_skew_enabled: true`,
  `crisis_skew_regime_gate_enabled: true`, `ic_seed_weighting: false`.
- Reverted vs parent: nothing — V225's term + flag stay; V226 adds the gate on top.

## Determinism plan

The gate adds **branching only — no new float-accumulation site**. The skew value
fsum (in `crisis_skew.py`) and the 2-element post-composite add fsum (V225,
`signal_generation.py:1245`) are unchanged. `_regime_gated_skew` is a pure boolean
branch returning either the V225 value or `0.0`. W is a constant swap. The gate
counters are integer-only (observability), never a numeric path. Expectation:
**6/6 hermetic at $0.00**. Any FAIL ⇒ auto trade-diff (obs #14) names the field.

## Observability shipped with V226

1. **Per-cycle gate-decision log** (`gate_decision={accept|skip}, regime_label,
   cycle_idx`) at the application site — one-grep confirmation of *when* the gate
   suppresses the term, per snapshot. This is the instrument that would have caught
   V225's "fires every cycle" pathology at smoke time instead of post-grid.
2. **`skew_gate_accept_cycles` / `skew_gate_skip_cycles`** in `results.json`
   observability — the per-snapshot accept/skip distribution, read by the
   gate-aware cell-identity assertion.

## Falsifier table (pre-committed fork — no moving goalposts)

Decisive comparison = **skew-ON+gated vs within-grid skew-OFF equal-weight control**
(same commit + caches), per gate. Both arms IC-off. Anchor baselines (V225
within-grid equal-weight controls, same harness): trend −$217.71 (23t),
crisis −$3,519.91 (31t), recent +$4,901.01 (22t).

| # | Outcome | Reading → V227 |
|---|---|---|
| **1** | **Crisis Δ > +$200 AND trend/recent within ±$200** | **SHIP.** The regime-gate makes the crisis brake conditional and it works. Update README crisis baseline if a record breaks. **V227 = stack with the banked trend-IC overlay (+$2,206 hermetic, V223).** |
| **2** | Gate brings `skew_on_cycles` < 40 on trend/recent **but** crisis Δ within ±$200 | Signal too weak even when gated (realized proxy lacks the lead of implied skew). **V227 escalates to a paid implied-skew vendor** (Tardis/Amberdata Deribit 25-delta IV) with the V215 freeze recipe. |
| **3** | Gate **still** leaves `skew_on_cycles > 40` on trend/recent | Gate semantics or regime-label distribution wrong (the 1-cycle-lag / VRP-mapping assumption fails). **V227 = forensics on regime-label firing per snapshot** (which cycles read crisis/high_vol on trend/recent and why); tighten to a drawdown-magnitude threshold. |
| **4** | **Trend OR recent regresses > $200** | The directional brake is harmful even gated (it bites in mislabeled cycles or the gated subset still over-shorts). **Revert + escalate to paid vendor.** |
| — | Any 6-cell determinism FAIL | New branch opened a near-boundary channel (unexpected — no new sum). Auto trade-diff names the field; fence or back out before reading PnL. Falsifier = "all cells PASS". |

## Pre-committed risks + mitigations

- **Regime label never reaches {crisis,high_vol}** (gate skips always → fork 2/3).
  Mitigated by sourcing the *authoritative VRP-mapped* `_regime` (not the orchestrator
  context regime, which is fed a different key and may stay `normal`). The smoke test
  (Step 4) verifies `skew_on_cycles` varies per snapshot *before* the grid.
- **1-cycle lag mislabels the transition cycle.** Immaterial for persistent regimes;
  fork 3 surfaces it if it isn't.
- **Cell-identity FAILs on gated-to-zero skew-ON cells.** Mitigated by the gate-aware
  `--expect-gate on` mode (relaxes the >0 inertness check to "gate was evaluated").

## Gate results

Grid ran 2026-06-18 06:51→14:46Z (commit `ead8f2a` HEAD, sleep=10, N=2, 200
cycles/replicate). Decisive comparison = **skew-ON+gated (W=0.2) vs within-grid
skew-OFF equal-weight control**, same commit + caches, both IC-off.

| Gate   | skew-ON gated (W=0.2) | skew-OFF control | **Δ (ON−OFF)** | WR (ON/OFF) | PF (ON/OFF) | long/short ON | skew_on_cycles | gate accept/skip | Determinism |
|--------|----------------------:|-----------------:|---------------:|:-----------:|:-----------:|:-------------:|:--------------:|:----------------:|-------------|
| trend  | **−$3,781.72** (27t)  | −$1,330.48 (23t) | **−$2,451.24** | 0.30 / 0.30 | 0.563 / 0.812 | 9 / **18** | **91** | 97 / 103 | both PASS $0.00 |
| crisis | **−$4,188.88** (32t)  | −$3,621.25 (31t) | **−$567.63**   | 0.44 / 0.45 | 0.741 / 0.777 | 19 / 13    | **77** | 79 / 121 | both PASS $0.00 |
| recent | **+$4,975.56** (25t)  | +$3,279.02 → +$4,901.01 (22t) | +$74 to +$1,696 (control non-det) | 0.44 / 0.50 | 2.191 / 1.800 | 18 / 7 | **113** | 113 / 87 | ON PASS $0.00; **OFF FAIL $1,621.99** |

Result files: `data/v226_skew_{on,off}_<gate>_determinism/`. Cell-identity:
**12/12 PASS** (gate-aware `--expect-gate on` confirmed the gate was *evaluated*,
not silently inert). Determinism: **11/12 replicate-pairs hermetic at $0.00**; the
sole FAIL is the **skew-OFF recent control** (see Conclusion §3).

## Conclusion

**REVERT. The regime gate fails its purpose and the gated brake is still harmful.**

1. **Fork #4 (revert) fires decisively.** Trend gated-skew-ON (−$3,781.72) regresses
   **−$2,451** vs the within-grid equal-weight control (−$1,330.48), both byte-deterministic
   — no ambiguity. The mechanism is unchanged from V225: ON adds **4 net shorts** on trend
   (long 9 / short 18 vs control's 9 / 14) — the downside-semivariance term is a directional
   bearish tilt that the gate failed to switch off.

2. **The crisis thesis is refuted.** crisis ON−OFF = **−$567.63** — the gated brake made the
   *target* gate **worse**, not the predicted +$200…+$2,000 better. A realized-semivariance
   "crisis brake" doesn't help the crisis gate even when conditioned and down-weighted; the
   crisis loss is entry-timing/sizing, not a missing directional overlay (V225 parking-lot
   confirmed). The realized proxy is now **twice-refuted** (always-on V225 + gated V226).

3. **Fork #3 also fires — the gate barely suppresses.** `skew_on_cycles` = **91 (trend) /
   113 (recent)**, both ≫ the 40 no-harm threshold; the gate accepted **97/79/113** of 200
   cycles per gate. The 1-cycle-lagged VRP→regime label classifies **~half** of the
   trend/recent windows as {crisis, high_vol} — the "trend/recent are mostly normal"
   assumption (V226 hypothesis) is **wrong**. The gate is not a clean crisis selector; it's
   a coin-flip that leaks the harmful tilt into ~half of every window.

4. **Incidental determinism regression (NOT crisis_skew, carried to V227).** The **skew-OFF
   recent** control FAILED determinism ($1,621.99 spread; r1 +$3,279.02 vs r2 +$4,901.01,
   NEARUSDT long entry flipped cycle 95↔93). This is in the **skew-disabled** arm — the
   V226 prior-cycle regime injection (`victoria_node.py:948`) writes an attribute read *only*
   inside the `crisis_skew_enabled` block, so it is inert here and cannot be the cause. r2
   reproduces the V225-era recent control (+$4,901.01) **exactly**; r1 diverged. This is the
   recurring **signal-layer float-ordering "4th channel"** (V216→V221 lineage) resurfacing on
   the 2026-06-18 recent snapshot — a real but orthogonal determinism channel. (The trend OFF
   control drift −$217.71→−$1,330.48 vs V225 is the same family: cross-snapshot / 2-day cache
   drift on a flag-OFF-inert HEAD.)

**Revert action:** both `crisis_skew_enabled` and `crisis_skew_regime_gate_enabled` already
default OFF (`features.py:711,724`) and the gate code is inside the flag block — main's
equal-weight path is numerically unchanged. **No code revert needed; nothing to back out.**
The V226 machinery (gate helper, gate-decision log, gate-aware cell-identity) stays dormant —
reusable if a *forward-looking* skew source ever lands.

## V227 brief — abandon the realized proxy; vendor implied skew + determinism forensics

The realized downside-semivariance proxy is **twice-refuted** as a directional overlay; stop
iterating on it. Two parallel tracks:

- **Track A (the skew thesis, last attempt — fork #4/#2 escalation): paid implied-skew
  vendor.** Source forward-looking Deribit **25-delta risk-reversal / DVOL** (Tardis.dev or
  Amberdata) for BTC/ETH, snapshot-freeze it via the V215 recipe (`OMEGA_FROZEN_CACHE` guard
  + `data/frozen_*` JSON), and re-run the *same* gated grid. Implied skew **leads** realized
  semivariance — the V225/V226 failure may be entirely "the proxy lacks the lead," which only
  a real implied series can settle. Pre-commit the **same** falsifier table. **If a paid
  implied series also fails to beat equal-weight on crisis → close the crisis-overlay program
  entirely** and bank equal-weight as the crisis main.
- **Track B (regime-label forensics — fork #3 root cause): why does the gate fire ~half the
  cycles on trend/recent?** Per-snapshot histogram of `_cycle_regime_label` over the 200
  trend/recent cycles — which cycles read {crisis, high_vol} and from what VRP value. The
  1-cycle-lag is not the issue (regimes persist); the **VRP→regime mapping threshold**
  (`_VRP_REGIME_MAP`, `victoria_node.py:~1062`) is mislabeling normal cycles. Tighten the
  enable condition to a **drawdown-magnitude threshold** (e.g. basket realized DD > Xσ)
  rather than the categorical VRP label — a stricter gate is the only way the realized proxy
  could ever satisfy fork #1, and it's free insurance for Track A's vendor signal too.
- **Track C (determinism, independent priority): close the resurfaced recent "4th channel."**
  The skew-OFF recent FAIL ($1,621.99, NEARUSDT cycle 95↔93 entry flip) is the V216→V221
  signal-layer float-ordering channel back on the 06-18 snapshot. Run `fingerprint_diff.py` +
  `trade_field_diff.py` on the two recent-OFF replicates to localize the field; it's a clean
  determinism bug independent of the crisis work and a prerequisite for trusting any future
  recent-gate comparison.

**The banked trend-IC overlay (+$2,206 hermetic, V223/V224) remains the only positive durable
finding** — parked for a trend-only application, orthogonal to all crisis/skew work above.
