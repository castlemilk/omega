# V217 — Close the third (signal-layer float/ordering) determinism channel + ship the field-naming fingerprint

**Date:** 2026-06-08
**Author:** claude
**Parent:** V216 (`322617e`) — sizing-side wall-clock channel CONFIRMED + FIXED.
Bar-time threading collapsed the trend determinism gate from $2,584 (ON) / $2,717
(OFF) to **exactly $0.00 in both selector arms** — the first byte-identical trend
gate at sleep=10 in the V207→V216 arc. The 3-gate × {ON,OFF} audit then surfaced a
**THIRD channel**: 4/6 cells byte-identical ($0.00) but **recent-OFF FAILs ($995)**
and **crisis-ON FAILs ($1,790)**. `fingerprint_diff.py` localized it: the per-cycle
signal `fp` hash diverges at cycle 1 (recent-OFF) / cycle 2 (crisis-ON) while *every*
named scalar — all `*.value` signals and all `_*` metrics — is byte-identical **at the
rounded-to-12 dump**, so it is a **signal-layer full-precision-float / iteration-
ordering** non-determinism (the V211 residual "4th channel," now dominant).

**Status:** PRE-REGISTRATION (gate results below are blank until the runs complete).

## Reflection-trigger check (mandatory per skill)

Trigger 1 (stagnation: V213→V216 broke no high-water) **technically fires**, but
`REFLECTION_V215.md` already discharged it for this whole arc: V212→V216 is a
**converging determinism-diagnosis arc**, not a stuck parameter walk. Each version
closed a *named* channel (V213 refuted the basket sort; V214 localized the network
leak; V215 fixed it + localized the sizing-time channel; V216 fixed that + named the
signal-layer channel). V217 closes the channel V216 named — the opposite of the dead
parameter-walk the trigger guards against.

Triggers 2/3/4: not fired. The eval is *becoming* hermetic (4/6 cells already $0.00),
which is the precondition for trustworthy deltas, not evidence of hidden coupling.
**Falsifier clause (carried from V216):** if V217 cannot localize the channel even
after the per-field tool ships, OR if 6/6 is not reached and the residual is not a
single nameable field, that is a genuine methodology problem (serial channel chase)
and triggers a fresh reflection — see "Methodology lesson" below.

## Hypothesis

**The third channel is a full-precision floating-point reduction whose summation
order is non-deterministic across processes** — the same class as the V211
basket_std / basket_mean fix, but in whichever signal field the per-field hash names.
Because `PYTHONHASHSEED=42` is pinned, this is **NOT** ordinary str-keyed `dict`/`set`
iteration (that is already deterministic at a fixed seed). The surviving cross-process
sub-12th-bit drift is textbook **float non-associativity over an `id()`-ordered
collection** — a `set`/`dict` of *objects* (identity hash, memory-address-ordered,
not seed-controlled), or a numpy/array reduction whose build order varies by process
memory layout. Threaded through the composite, the last-bit drift only changes a
**trade decision** where a conviction sits within ~$1k of an entry threshold — which
is exactly why **crisis-ON** and **recent-OFF** fail while their opposite arms and
trend (post-fence) do not: only those cells carry a borderline trade near the flip.

**Two-step plan (observability first, then fix):**

1. **Ship the per-field full-precision fingerprint** (the cheapest lever). The current
   `signal_fingerprint.jsonl` *proves* divergence (`fp` diverges) but the dump rounds
   every value to 12 places, so `fingerprint_diff.py` hits the dead-end
   `(fp differs but no scalar value differs)`. A per-field IEEE-754-bit-exact hex hash
   makes the next bisect **one command**: it names the `(cycle, signal_name)` whose
   bits drift. **This tool is guaranteed to localize** — `fp` is a pure function of
   exactly those per-field values (`!r` round-trips bit-exact), so if `fp` differs
   while key-sets match, at least one field's IEEE bits differ by construction. The
   per-field diff *cannot* reproduce the V216 dead-end.

2. **Localize + close the channel.** Bisect V216's failing cells (recent-OFF, crisis-
   ON) with the per-field tool → name the field → trace *its* computation → apply a
   `sorted(..., key=<value-stable>)` canonical order at the order-dependent reduction
   (V211 method), OR, if the named field's computation has no order-dependent
   reduction, describe and fix the actual resolution-sensitive source (do **not** force
   a sort that merely *looks* load-bearing — the V213 trap).

### Note: brief said "(ticker, signal_name)"; the data is market-level

The task brief specified a per-`(ticker, signal_name)` artifact. The diverging `fp` is
over the **market-level `last_signals` dict** (btc_dominance, vrp, carry, … + `_*`
metrics) — there is **no ticker dimension** in that dict. Synthesizing a ticker the
data lacks would add empty columns and muddy the `cmp`. So the artifact keys on
`(cycle, signal_name)`; the brief was written pre-data and the channel is market-level.
If the named field later traces to a *per-ticker* reduction, the trace goes into
`_last_ticker_decisions` at that point — per-ticker fingerprinting is not needed to
localize.

## Changes (planned)

- `scripts/run_training.py` — emit `data/{version}_per_field_fingerprint.jsonl`: one
  line per `(cycle, signal_name)` with `value_hex = struct.pack('!d', float).hex()`
  (IEEE-754 double, bit-exact), entries sorted by `(cycle, signal_name)` so the JSONL
  is line-comparable via `cmp`. Guarded (never breaks a run).
- `scripts/per_field_diff.py` — consume two per-field artifacts, report the first
  divergent `(cycle, signal_name)` and the hex bits of each side. One bash invocation.
- The signal-computation file the bisect names (TBD after Step 4) — minimal
  canonical-order fix.

## Pre-registered acceptance and falsifiers

Acceptance condition is the **6/6 hermetic gate**: 3 gates × {ON, OFF} all at
**$0.00 spread at sleep=10** (N=4 ON, N=2 OFF — the V216 protocol). This is also the
hermetic baseline + clean selector Δ (the determinism gate and the audit are the same
runs: a cell at $0.00 yields the verdict, the baseline PnL, and the ON−OFF Δ at once).

- **Acceptance / channel closed:** all 6 cells byte-identical at sleep=10 →
  **matrix mode unlocks** (V218). Record the hermetic baseline (selector OFF) and the
  honest selector Δ per gate.
- **Falsifier 1 — some cell still FAILs at sleep=10:** the per-field tool names the
  next divergent `(cycle, signal_name)` instantly → that is the NEXT channel. **Action:
  STOP, report the named channel in this doc, queue V218 = close it.** Do NOT chase a
  fourth channel inside V217, and do NOT start the matrix.
- **Falsifier 2 — per-field tool itself fails to name a field** (diverges with no
  field differing): impossible by construction (see hypothesis step 1); if observed,
  the fingerprint emission has a bug (e.g. a non-scalar field excluded from the hash
  but included in `fp`) — fix the tool, not the strategy.
- **Trade-count watch (reported observable, not a falsifier):** the V216-era daily-bar
  ~0.375× sizing regime persists; if a fix shifts trade counts below the floor (≥20),
  report it.

## Methodology lesson (pre-registered, to codify in the skill)

If V217 closes the named field and a *new* field immediately appears at the next gate
run — and this keeps happening one field at a time — we have a **serial channel-chase
pattern**, not a finite set of parallel channels. The codification: **determinism
fences must be validated at BOTH selector ON and OFF, not just ON** (V216 validated the
sizing fence only on the trend gate where both arms happened to pass, and the audit
later revealed two *other* cells still failing). The per-field tool makes each chase
step one command, but if the chase does not terminate after this step, the skill's
reflection section must add: *"a determinism fix is not 'done' until the full 6-cell
{3 gates × ON/OFF} grid is byte-identical — a single passing gate is not sufficient
evidence."*

## Gate results

All runs: seed=42, PYTHONHASHSEED=42, frozen cache (V215 HTTP guard + V216 bar-time
fence + V217 third-channel fix), per-run state restore, sleep=10, via
`scripts/check_determinism.sh` (wall-clock tripwire PASS at preflight).

### Step 4 — per-field bisect (the channel)

_(filled after Step 4)_

### Step 6 — 6-cell determinism gate @ sleep=10 (acceptance + hermetic baseline)

| Gate | Selector | N | PnL (replicates) | Trades | Spread | Verdict |
|------|----------|--:|------------------|-------:|-------:|---------|
| recent | OFF | 2 |  |  |  |  |
| recent | ON  | 4 |  |  |  |  |
| trend  | OFF | 2 |  |  |  |  |
| trend  | ON  | 4 |  |  |  |  |
| crisis | OFF | 2 |  |  |  |  |
| crisis | ON  | 4 |  |  |  |  |

### Hermetic baseline + honest selector Δ (deterministic)

| Gate | Selector OFF (baseline) | Selector ON | Selector Δ (ON−OFF) |
|------|------------------------:|------------:|--------------------:|
| recent |  |  |  |
| trend  |  |  |  |
| crisis |  |  |  |

## Conclusion

_(filled after the runs)_

## Next steps

→ **V218 = matrix mode** (cells A carry plumbing / B V170 IC / E snap_crisis_2020q1),
shared baseline = V217 OFF — **only if 6/6 PASS**. Otherwise V218 = close the next
named channel.

Open questions / parking lot:
- Regime-gated selector (V219, after the matrix): if the clean recent/crisis ON−OFF Δ
  stays positive on the hermetic eval, consider a per-gate selector toggle; if it
  collapses to noise, drop the selector permanently.
- Daily-bar 0.375× sizing regime vs cadence-gating the intraday windows (V216 fork) —
  orthogonal to determinism, shapes the matrix reference baseline.
- `regime_signal_weighting` + `hmm_regime` undeclared no-ops (carried).
