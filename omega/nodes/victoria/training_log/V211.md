# V211 — Sort `signals.items()` in basket_std and basket_mean aggregations

**Date:** 2026-06-03
**Author:** claude
**Parent:** V210 (commits `26dcf21`, `eed0c7b`) — localized the second eval-noise channel to two `signals.items()` floating-point aggregations at `strategy.py:1722` (basket_std) and `strategy.py:2130` (basket_mean). Sub-signal layer (`signal_contribs.jsonl`) verified byte-clean. Same fix pattern as V208a (`_construct_portfolio` sort), at the basket-aggregation layer upstream of conviction filtering.
**Status:** running

## Hypothesis

Adding `sorted(..., key=lambda kv: kv[0])` to the `signals.items()` traversals at `strategy.py:1722` (basket_std) and `strategy.py:2130` (basket_mean) collapses the recent within-pair spread to <$200 with trade Δ ≤ 1 on a 2-pair audit. Trend collapses to <$200. Crisis stays at $28 ± $28 (no expected change — crisis is already decision-stable per V210's selection-stability finding).

## Changes

- Files touched: `omega/nodes/victoria/strategy.py` only
- Two surgical sort wraps:
  - `:1722` — `for t, sig in signals.items()` (basket_std list comp) → `for t, sig in sorted(signals.items(), key=lambda kv: kv[0])`
  - `:2130` — `for t, sig in signals.items()` (basket_mean list comp) → `for t, sig in sorted(signals.items(), key=lambda kv: kv[0])`
- Configs / preset name: none
- Feature flags toggled: none
- Anything reverted vs parent: none
- Semantic change: none — strict canonicalization, same pattern as V208a's 8 `_construct_portfolio` sort wraps

## Audit procedure

12 sequential runs, seed=42, PYTHONHASHSEED=42, frozen funding cache:

- 2 pairs × 3 gates × {r1, r2}
- Outputs: `data/v211_audit/<gate>_p<n>_<r|r2>/`
- Per-pair within-pair byte diff: `metrics.jsonl` (timestamp-stripped), `results.json` PnL Δ, `trades.csv` row-by-row
- Cross-pair consistency: do p1 and p2 give identical numbers (they must, with seed+PYTHONHASHSEED pinned post-fix)

## Falsifiers (pre-registered)

| Outcome | Interpretation | V212 branch |
|---|---|---|
| recent + trend both collapse to <$200 spread, Δ ≤ 1, crisis unchanged | Composite-formation sort is the third (and final) fence. Three fences jointly sufficient. | Re-baseline V204 / V205 / V206b single-seed headline numbers at the new noise floor. Finally re-enter the `strategy_selector_enabled` question. |
| recent collapses but trend doesn't | A *fourth* channel exists in the trend-specific snapshot path. | V212 = cycle-1 bisect on trend's residual signal. |
| recent doesn't collapse (still >$1,000 spread) | Sort isn't the channel OR there's a parallel unsorted aggregation we missed. | V212 = wider grep for `.items()` / `for X in dict` in the composite-formation path. |
| Crisis regresses >2σ ($56) | Sort broke a downstream invariant. Revert V211 immediately. | V212 = surgical fix on `:1722` only (skip `:2130`) and retest. |

## Gate results

| Gate    | r1 PnL | r2 PnL | Spread | Trade Δ | Verdict |
|---------|-------:|-------:|-------:|--------:|---------|
| recent  |        |        |        |         |         |
| trend   |        |        |        |         |         |
| crisis  |        |        |        |         |         |

Per-pair detail filled below post-audit. Result files under `data/v211_audit/`.

## Conclusion

(filled after audit completes)

## Next steps

→ **V212**: TBD — chosen per falsifier table above based on V211 outcome.
