# V279 — Make signal-family inertness visible at startup

**Status:** PRE-REGISTERED (this document committed before any code change)
**Date:** 2026-08-27
**Author:** claude
**Parent:** [`V279_PHASE0_CROSS_ASSET_INERTNESS.md`](V279_PHASE0_CROSS_ASSET_INERTNESS.md) §5
**Standing baseline (MUST NOT MOVE):** crisis **+$599** / trend **+$2,997** / recent **+$30**
**Class:** observability. **No strategy code. No new signal. No new threshold. No grid.**

---

## §1 — Hypothesis

Phase 0 established that all four members of the `cross_asset` signal family are exactly
`0.0` in every frozen backtest, that none is ever injected into a ticker's signal dict
(`signal_generation.py:1223-1230` inserts only non-zero values), and therefore that the
family contributes nothing to the standing baseline while `AdaptiveCombiner` computes a
family weight over an empty set.

It took a four-step probe to learn that. The V213 startup banner — built *precisely*
because the V148–V202 arc burned four-plus versions tuning subsystems that never ran —
did not surface it, because it reports **flag wiring** (`UNDECLARED — getattr→False`,
`ON · module importable → ACTIVE`) and says nothing about a correctly-declared,
correctly-imported signal whose **value** is identically zero.

> **The bet:** the same class of finding — *"this thing you think is contributing is
> contributing nothing"* — is one aggregation away from being a single greppable line,
> over data the run already computes.

**Falsifier (G3):** if the report does not independently mark `cross_asset` **INERT** in
a real frozen run, it does not reproduce the Phase 0 finding and is not worth shipping.

## §2 — What this version is NOT

- **NOT a fix for the inertness.** `vix_signal`/`spy_signal` are zero *by correct design*
  (the V226/V227 determinism fences); unfencing them would reintroduce a live-HTTP leak
  into frozen runs. Phase 0 §5 says this explicitly. The goal is that inertness is
  **visible**, not that it is removed.
- **NOT a change to any signal, weight, threshold or gate.** Logging only.
- **NOT a second family map.** The report reads
  `AdaptiveCombiner.SIGNAL_FAMILIES` (`adaptive_combiner.py:48`), the existing source of
  truth. Duplicating it would create exactly the declared-vs-actual drift V276/V277 spent
  two versions closing.

## §3 — Change surface (2 files)

1. `omega/nodes/victoria/signal_generation.py` — a `_report_family_inertness(signals)`
   helper, called **once per process** immediately after the per-ticker loop closes
   (`:1491`, where `signals` is complete and before the BTC-beta block mutates it). For
   each family it logs member count, live-member count, and the token **`INERT`** when no
   member is present-and-non-zero on any ticker. Prefixed `[startup]` so it greps
   alongside the V213 banner. `AdaptiveCombiner` is already imported at `:256`, and
   `adaptive_combiner.py` imports nothing from victoria, so there is no circularity.
2. `tests/test_family_inertness.py` (**new**) — §4.

**Not touched:** `adaptive_combiner.py`, any signal module, `strategy.py`, `features.py`,
any grid script, `data/standing_baseline.json`.

## §4 — Gates

| Gate | Assertion |
|---|---|
| **G1** (unit, both directions) | Given a synthetic `signals` dict: a family whose members are all absent-or-zero is marked **INERT**; a family with one live non-zero member is **not**. A report that can only ever say INERT is worthless — both directions are asserted (the V275 "gates that cannot fail" lesson). |
| **G2** (no behaviour change) | The three sentinels reproduce this host's values **to the cent** — crisis $1,082.80 / trend $4,679.67 / recent $824.81. This version writes log lines; if PnL moves, the hook has side effects and that is a **STOP**. |
| **G3** (the falsifier — end-to-end) | In a real frozen run the report marks `cross_asset` **INERT** *without being told to*, reproducing Phase 0's finding as one line. Also required: it must **not** mark every family INERT — `momentum` must show at least one live member, since `sma_crossover` demonstrably trades. |
| **G-DET** | $0.00 spread, N=2, at the V240 condition (`sleep=0`, `cycles=60`). |

**Anti-Goodhart:** there is no number this version can win. G2's only passing outcome is
"unchanged"; G3's is "it independently rediscovers a finding we already have on paper."

---

## §5 — Results

_(empty at pre-registration time)_

## §6 — Conclusion

_(pending)_

## §7 — Next steps for V280

_(pending)_
