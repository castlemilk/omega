# V208 — Eliminate the third noise channel (sizing-chain iteration order)

**Date:** 2026-06-02
**Author:** claude
**Parent:** V207b (commit `5b1e1ee`) — channel localized to `strategy.py:_construct_portfolio` sizing chain (lines 3143-3192), most-likely root `_compute_weighted_conviction` rolling z-score over `_signal_history` keyed on signal-name only.
**Status:** running

## Hypothesis

V207b's static cycle-1 bisect localized the divergence to the
sizing-multiplier chain in `_construct_portfolio`. Per-signal traces,
composites, demeaned composites, conviction scores, and basket stats
are all byte-identical at every shared trade event. The divergence is
a downstream **2× clip-boundary discontinuity** (most likely
`_conv_scale = max(0.5, min(2.0, _w_conv / 0.25))` at lines 3142, 3150,
3173, 3182).

The mechanism: `_compute_weighted_conviction`'s rolling z-score uses
`self._signal_history` keyed on signal-name only, mutated by every
candidate in every cycle. The mean/std at the moment any one symbol
is normalized depends on the iteration order across symbols. A tiny
order-dependent difference in `_w_conv` straddles 0.5 or 1.0 on the
clip, producing exact 2× sizing.

**V208 is methodology-only.** No new feature. The deliverable is a
byte-identical no-op pair on the recent gate, in service of restoring
a trustworthy eval. Walk the four V207b-registered branches in cost
order, stop when the within-pair spread collapses (< $100 with
identical `total_closed`).

## Falsifier branches (V207b pre-registered)

| Branch | Hypothesis | Cost |
|--------|-----------|-----:|
| **A — canonical iteration order** | Iteration order alone is the channel. Sort every `(long\|short)_candidates.items()` (and dict-comprehension equivalents) in `_construct_portfolio` by ticker. | zero |
| **B — per-symbol `_signal_history`** | Cross-symbol history pool is the contamination vector. Re-key on `(ticker, signal_name)`. | low |
| **C — disable `live_signal_normalization`** for audit | Confirms the normalization IS the channel, vs. another multiplier. | low |
| **D — per-multiplier instrumentation + rerun** | If A→C all fail, log every multiplier per ticker per cycle and bisect. | high |

## Changes (sub-experiment A — applied)

- Files touched: `omega/nodes/victoria/strategy.py`.
- In `_construct_portfolio` (lines ~2983-3192), every iteration over
  `long_candidates.items()` / `short_candidates.items()` and every
  dict-comprehension over those was wrapped in `sorted(..., key=lambda kv: kv[0])`.
- Wide net: 8 call sites in the multiplier chain + risk_parity +
  momentum + resilience cap + filtering.
- Feature flags: none changed.

## Verification protocol

- Two runs on recent gate, both `--seed 42`, V204 resilient launch
  pattern (`nohup … &; disown`).
- Byte-diff: `md5sum` on timestamp-stripped `metrics.jsonl`.
- PnL delta < $100 from `results.json`.
- `total_closed` identical.
- Spot-check `trades.csv`.

## Baseline (V207a recent pair, parent)

- r1: PnL -$3,452.23, 78 trades.
- r2: PnL -$195.17, 74 trades.
- Spread: **$3,257.06**; trade Δ: 4.

## Gate results

| Sub-exp | Spread before | Spread after | Trade Δ | Channel found? |
|---------|--------------:|-------------:|--------:|:--------------:|
| A — canonical sort        | $3,257.06 | | | |
| B — per-symbol history    | | | | |
| C — disable live_norm     | | | | |
| D — per-mult instrumented | | | | |

Result files: `data/v208a_recent_{r1,r2}_results.json` etc.

## Conclusion

(filled in after runs)

## Next steps

→ **V209**: if A passes, redo full V206b 3-gate × 2-pair audit at the
new noise floor, then re-enter the `strategy_selector_enabled` flag
question that V199–V205 was trying to answer.
