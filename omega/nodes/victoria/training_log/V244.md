# V244 — corr-spike portfolio tail cap (drop-only sizing throttle)

**Date:** 2026-07-13 · **Author:** claude (Fable 5)
**Parent:** V241 (REFUTED — reasoning layer OFF). Standing baseline: **V240
selective universe** — crisis +$599 / trend +$2,997 / recent +$30 (32-window
walk-forward means, `origin/main` @ `ab85789`).
**Status:** PRE-REGISTERED (results blank until the grid completes).
**Source of hypothesis:** `REFLECTION_V241.md` §5 top untouched dimension;
execution spec inherited from the V237 historical pre-registration (corr-spike
detector, `raw_weights` actuator site). V242/V243 were consumed by parallel
Opus tracks (both refuted/parked); this is the next sequential pre-reg.

## Hypothesis (one sentence)

When realized trailing pairwise correlation across the traded book spikes above
a threshold τ, capping NEW-entry position sizing by a factor s (drop-only,
never up-size, existing positions untouched) compresses the correlated
grind-down that dominates the pooled left tail — tightening pooled p25 while
preserving or lifting means, particularly on recent where tail losses dominate
(+$30 mean, p25 deep negative).

## Why this bet (user's frame: "get profitable reliably")

V241 failed precisely on the tail conjunction (p25-Δ −$705) while injecting
$27,345 of pooled spread. The tail dimension has never been attacked directly:
the entire V148→V241 arc is entry/selection/sizing on per-name signals. This is
the first *portfolio-level risk control* measured on the full distribution.

## Design

- **Detector:** rolling mean pairwise correlation of daily close-to-close
  returns over a trailing **7-bar** window (V237 spec) across the ACTIVE
  universe (selective, blacklist {BTC,DOT,LINK} excluded from the book but BTC
  present in every snapshot), computed from the same frozen OHLCV the strategy
  already holds. Bar-time only (V216 fence); `math.fsum` for every cross-ticker
  reduction (V221 fence). Note: a strict 60-bar trailing corr is NOT computable
  at early-window entry bars (windows are 91 bars, entries begin ~bar 28) —
  this, not missing OHLCV, is the real constraint behind the V243 caveat; the
  frozen walk-forward snapshots DO contain full per-symbol OHLCV.
- **Trigger:** detector value > τ at the entry bar.
- **Actuator:** drop-only multiplicative cap on new-entry weights at the
  retained V234/V237 site — `raw_weights` normalization in `strategy.py` —
  `weight *= s` while triggered. Whole-book, name-blind by construction.
- **Flags:** `portfolio_corr_cap_enabled` (default **OFF** ⇒ byte-identical to
  standing main; unit-verified), `portfolio_corr_cap_threshold` (τ),
  `portfolio_corr_cap_scale` (s), `portfolio_corr_cap_lookback` (7).
- **Phase-0 tracer (mandatory, V241 lesson — activity measured before
  quality):** per-cycle log of {corr observed, τ, fired?, cap magnitude,
  entries affected} to `$OMEGA_AUDIT_OUTPUT_DIR`-relative JSONL.

## Protocol (in order; each step gates the next)

1. **$0 offline calibration BEFORE any grid** (standing V234 separator rule +
   REFLECTION_V241 §4 gate on tail-targeted bets). Counterfactual scorer on the
   EXISTING V240 confirm ledgers (32 windows, `universe_selective`, certified
   deterministic): recompute the detector at each trade's entry bar
   (entry bar = cycle − hold_cycles + 28, validated V236) from the frozen
   snapshots; counterfactual PnL = pnl × s for trades entered while corr > τ
   (linear in size — drop-only cap scales the ledger linearly). Sweep
   τ ∈ {p60, p70, p80, p90} of the pooled per-bar detector distribution ×
   s ∈ {0.5, 0.7, 0.9}. Report per-regime mean/p25/p50/p75 deltas per cell.
   *Deviation note:* the launch prompt referenced "V243's committed data" —
   V243 lives on an unmerged branch (`v243-portfolio-sep`, out of scope);
   the V240 confirm ledgers are the committed 32-window base. The prompt's
   window-level P&L-stream correlation is reported as a sensitivity check,
   not the primary detector.
2. **Sanity gate (pre-registered):** ≥1 (τ, s) cell must show pooled p25-Δ
   > +$100 AND recent mean-Δ > +$100 on the offline scorer, else **V244 is
   REFUTED at scoring** — no grid burn; write the REFUTED doc and branch to the
   secondary candidate (gdelt solo cell).
3. **Implementation** (only if gate passes): flag-gated, OFF byte-identical.
   Smoke: OFF arm reproduces a V240 confirm cell byte-identical.
4. **32-cell frozen walk-forward grid**, ON vs the V240 confirm OFF baseline
   (V241 join pattern), top-2 surviving (τ, s) cells (matrix V244.A/V244.B if
   both survive scoring), N=2 sentinels per regime
   ({snap_wf_20230912, snap_wf_20240310, snap_wf_20250305}), artifacts to
   `/Volumes/gamma-systems-2/omega-victoria-data/v244/`,
   `TMPDIR=$OMEGA_AUDIT_OUTPUT_DIR/tmp`. Committed-state restore before each
   cell (V240 protocol). 4-window OFF baseline reproducibility spot-check
   before the grid.

## Falsifier (hard, pre-registered — verbatim from launch)

**V244 FAILS if ANY of:**

- recent mean-Δ < +$100 **AND** recent p25-Δ < +$400
- pooled p25-Δ < +$0 (the tail must not worsen — this is the entire point)
- trend mean-Δ < −$300
- crisis mean-Δ < −$300
- Phase-0 trigger-rate < 3% across all windows (cap never fires → inert)

→ flag stays OFF, revert-and-branch reflection.

**Determinism FAIL** ⇒ bisect/fence/re-run before any verdict (standard arc).
Matrix note: if two cells run (V244.A/B), only ONE may merge; many-comparisons
threshold noted per skill (2 cells ⇒ report both against the same bars, no
cherry-picking the better post hoc — both bars were fixed here).

## Out of scope

Per-name gates (8 refutations); realized-drawdown selectors (V234); any
reasoning-layer touch (`reasoning_layer.py`, `data/frozen_llm_cache/` —
V241 REFUTED, flag OFF, untouched); fetching/rebasing `v243-portfolio-sep`.

## Results

*(blank at pre-registration — filled after the grid)*

### Offline calibration (step 1–2)

TBD

### Phase-0 tracer

TBD

### Walk-forward distribution (32 windows, Δ vs baseline crisis +$599 / trend +$2,997 / recent +$30)

TBD

## Verdict

TBD

## Next steps

TBD
