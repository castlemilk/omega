# V205 — Disable V157 CRISIS regime signal weights

**Date:** 2026-06-01
**Author:** claude
**Parent:** V204 (revert to V172 baseline; new trend high-water +$22,105; crisis regressed to −$22,809)
**Status:** planned

## Decomposition of `1e743de` (the pre-registration discipline step)

Brief instructed: decompose `1e743de` and re-add only the IC-weighting / regime-aware composite architecture subset.

**Result: the brief's premise is inverted by the diff.** Documenting in full so the pivot is auditable.

1. `1e743de`'s strategy.py portion is **17 lines, +11 −6, purely cosmetic**:
   - Variable rename `_bear_T_exit` → `_bear_t_exit` (lines 1521-1538).
   - Multi-line reformat of two function calls (lines 1562-1565, 2647-2654).
   - No control flow, no IC weighting, no regime composite, no signal pruning logic. Zero architecture.
2. Line counts at endpoints:
   - daa8304 (V172): **3784 lines**.
   - 1e743de^ (pre-consolidation worktree state): **3361 lines**.
   - 431fc3f (V202): **3420 lines**.
   - HEAD (after V204 revert): **3784 lines**.
3. The 423-line shrinkage between V172 and 1e743de^ happened on a **divergent branch** (merge-base = `f2cff01`, NOT daa8304). The five visible commits on the linear path `daa8304..HEAD` add only **+59 lines net**.
4. Pickaxe on `_REGIME_SIGNAL_WEIGHTS`, `_apply_regime_signal_weights`, `_regime_ics`, `_signal_history` across `daa8304..98a0b26^`: **zero hits**. None of {1e743de, cbbfb07, 46297bb, 1f53e77, 431fc3f} touch those architecture symbols.
5. Inspection of `git diff daa8304 1e743de^ -- strategy.py`: **445 deletions, 22 insertions**. The deletions include V153, V155, V156, V157, V158/V161, V162, V164, V166, V168, V169b, V170, V172_pruned — i.e. essentially the entire V153–V172 architectural stack.

**Implication:** the V157/V166/V170 architecture the brief assumed was missing from current HEAD is in fact already there (V204's revert restored it). The V202-era crisis improvement (+$3,767) came from **deleting** this architecture on the worktree branch, not adding anything. The brief's V205 hypothesis ("re-add architecture from 1e743de") therefore cannot execute — there is nothing to re-add.

## Hypothesis (pivoted)

The V172 architecture restored by V204 contains multiple regime-aware suppressions/weights, any of which could be the source of the crisis regression (−$22,809 V172/V204 vs −$19,042 V202-era). The single most paper-suspicious component for the `snap_crisis_2022h1` snapshot is **V157's CRISIS regime weights** (`strategy.py:313-331`):

- Damps trend-following signals (breakout, ADX, timeframe, MACD, zscore) by 0.3–0.5× in CRISIS mode.
- Boosts mean-reversion signals (RSI, BB, Ollivier-Ricci) by 1.2–1.5× in CRISIS mode.
- Documented rationale (V158/V161 comments): "rely on mean-reversion (bb/rsi/ORC) for alpha. Fewer trades → V153-like behavior."

For the 2022 H1 crisis snapshot — a *sustained* downtrend, not a chop with mean-reversion alpha — damping the trend-following signals that would correctly say "short the breakdown" while boosting mean-reversion signals that incorrectly say "long the dip" is structurally wrong. Stripping the CRISIS branch should let trend-following signals operate at full weight during crisis and let the strategy short into the crash.

V205 = set `_REGIME_SIGNAL_WEIGHTS["CRISIS"] = {}` (one-line change). Keep TREND weights untouched. One bet, one regime branch, isolatable.

This is the symmetric counterpart of the brief's intent: instead of "re-add architecture to recover crisis", it's "strip the architectural component most likely to be hurting crisis, leaving the rest of V172's stack intact."

## Changes

- Files touched: `omega/nodes/victoria/strategy.py` (single dict, lines 313–331).
- Configs / preset name: default (`v93_baseline`).
- Feature flags toggled: none.
- Anything reverted vs parent: none (V204 = V172 baseline; V205 surgically prunes one dict branch).
- Seed: 42 throughout.

## Falsifiers (pre-registered, V203 2σ thresholds)

- **Crisis ≥ −$19,500** (recover ~$3.3K, within $500 of V202-era −$19,042) ⇒ **confirmed**: V157 CRISIS weights were the (or a major) crisis-hurter.
- **Trend ≥ +$22,103** (within 2σ of V204 baseline +$22,105) ⇒ no trend regression. Crucial — keeping the V204 trend ceiling is the whole point of the surgical approach.
- **Recent within ±$5,094** of V204 (−$4,472) ⇒ noise either way, ignore.

Branch decisions for V206:
- Crisis confirmed (≥ −$19,500) AND trend preserved ⇒ V206 = audit V170 per-regime IC weighting in isolation (next architectural component to test for further crisis recovery, or for adversely interacting with V157 strip).
- Crisis improves but trend regresses > 2σ ($2) below V204 ⇒ V206 = revert V205 and try a different architectural prune (V166 normalization, or V170 IC; do not stack with V157 strip).
- Crisis stays ≤ −$22,000 (no movement) ⇒ V206 = V199 carry plumbing in isolation (commit `cbbfb07` architecture subset, per V204 salvage shortlist row 2).
- Crisis between −$19,500 and −$22,000 (partial signal) ⇒ V206 = stack V205 strip with V170 IC disable to test additivity.

## Gate results

| Gate    | PnL | Trades | WR | PF | Max DD | Notes |
|---------|----:|-------:|---:|---:|-------:|-------|
| recent  |     |        |    |    |        |       |
| trend   |     |        |    |    |        |       |
| crisis  |     |        |    |    |        |       |

Result files: `data/v205_<gate>_results.json`, `data/v205_<gate>_trades.csv`.
Gate-pass report: `data/v205_<gate>_gate_result.json`.

## Conclusion

(filled after runs)

## Next steps

(filled after runs — selection from the four branch-decision arms in Falsifiers section)

## References

- V204.md — revert baseline + salvage shortlist (row 1 "1e743de HIGH priority" superseded by this decomposition).
- V172.md — original Ridge calibrator + signal pruning context for the architecture being audited.
- REFLECTION_V202.md — methodology for treating single-seed deltas under 2σ as noise.
- commit `daa8304` — V172 strategy.py state; surface contains the V157 dict being pruned.
- commit `1e743de` — V148+ consolidation; strategy.py portion is cosmetic, not load-bearing.
- merge-base `f2cff01` daa8304 ↔ 1e743de — proves the divergence point that hid the deletion.
