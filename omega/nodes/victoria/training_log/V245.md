# V245 — gdelt solo cell (frozen geopolitical feed on the selective baseline)

**Date:** 2026-07-13 · **Author:** claude (Fable 5)
**Parent:** V244 (REFUTED at scoring, $0). Standing baseline unchanged: **V240
selective universe** — crisis +$599 / trend +$2,997 / recent +$30.
**Status:** PRE-REGISTERED (results blank until the grid completes).
**Source of hypothesis:** REFLECTION_V241 §5 candidate #2; V244's
pre-registered refuted-branch target. Source frozen and smoke-tested in
V240.C (`V240_GDELT_ADD.md`) — never evaluated in-grid.

## Hypothesis (one sentence)

Enabling the frozen gdelt geopolitical signal solo
(`frozen_series_signals="gdelt"` + `geopolitical_signals`) on the standing
selective-universe baseline injects exogenous-shock information the OHLCV
composite cannot see (COVID-2020, Ukraine-2022 both max the frozen signal's
intensity/regime-shift; the crypto-endogenous 2024 unwind does not), lifting
the crisis/recent tail without the whale_flow-style trend tax (V240.B: the
V238 trend breach was ~90% whale_flow; the macro feeds were benign).

## Design

- **Zero new mechanism code.** All wiring shipped in V240.C
  (`GeopoliticalSignal.compute_from_series`, `geo_*_signal` composite
  aliases, fsum + degenerate-variance fences). V245 is a measurement cell:
  config `gdelt_solo` = standing selective config + `frozen_series_enabled` +
  `frozen_series_signals: "gdelt"` + `geopolitical_signals: true`. The other
  5 feeds stay on their live paths (== baseline code path), exactly the
  V240.B solo pattern.
- **OFF arm:** NOT re-run — the V240 confirm cells (`v240wf_*_universe_selective`),
  reproducibility re-certified 2026-07-13 by the 4-window spot-check.
- **Grid:** `scripts/v245_gdelt_grid.sh` (V241 pattern), 32 windows × 1
  config, N=2 sentinels {snap_wf_20230912, snap_wf_20240310, snap_wf_20250305},
  artifacts `$AUDIT/v245/` + `v245wf_*` cells, committed-state restore per
  cell, `TMPDIR=$AUDIT/tmp`. Aggregator `scripts/v245_wf_aggregate.py`
  (V241 join adapted) applies the falsifier below.

## Phase-0 (before the grid — V234 separator rule, additive-feed form)

An additive signal cannot be counterfactually scored on existing ledgers (it
changes the composite, not a filter on it), so phase-0 is a wiring + variance
probe, not an outcome scorer:

1. **$0 coverage/variance probe:** frozen gdelt series span every window
   (2019-12→2026-07 committed in V240.C); verify per-window signal values
   VARY (not constant/ABSENT) across all 32 windows from the frozen files.
2. **2-window wiring probe (~2 short runs):** ON vs OFF on
   `snap_wf_20220321` (Ukraine invasion inside the window — the signal's
   strongest face-valid discriminator) + `snap_wf_20250305` (recent
   sentinel). PASS = `geo_*_signal` keys composite-visible in signal traces
   AND ON ≠ OFF on ≥1 window (V213 inertness lesson). No wiring ⇒ no grid.

## Falsifier (hard, pre-registered — V244-launch structure)

**V245 FAILS if ANY of:**

- recent mean-Δ < +$100 **AND** recent p25-Δ < +$400
- pooled p25-Δ < +$0 (tail must not worsen)
- trend mean-Δ < −$300
- crisis mean-Δ < −$300
- phase-0 wiring probe shows gdelt never composite-visible (inert ⇒
  refuted-as-untested, wiring bug filed instead)

**ADOPT (default-ON)** only if no clause fires AND pooled mean-Δ > +$0.
Pass-without-adopt (e.g. tail improves but pooled mean flat-negative) ⇒ KEEP
FLAG-GATED with the measured distribution recorded. Determinism FAIL ⇒
bisect/fence/re-run before any verdict.

## Out of scope

Reasoning layer (OFF, untouched); `v243-portfolio-sep` branch; portfolio-corr
mechanisms (closed by V244 + V237 clause); multi-feed combinations
({dxy,yield_curve} subset is a separate queued cell).

## Results

### Phase-0 — PASS (both probes)

1. **Coverage/variance:** all 10 frozen gdelt series span 2019-12-01→2026-07-10
   (2,391 obs), non-degenerate variance in every window. One contiguous 17-day
   upstream gap (2025-06-15→07-01) clips 2/32 windows (snap_wf_20250603 in-window,
   snap_wf_20250718 warmup-only) — honest-ABSENT cycles by the V240.C contract.
2. **Wiring (2 windows):** `geo_event_intensity_signal`, `geo_sentiment_signal`,
   `sanctions_signal` composite-visible on every traced trade;
   ON ≠ OFF on both windows (Ukraine window −$107.66 vs −$24.81 with a 13↔12
   trade flip; recent sentinel $426.51 vs $771.98, N=2 byte-identical).

### Walk-forward distribution (32 windows, Δ = gdelt_solo − selective baseline)

64 cells joined, **0 missing, 0 determinism FAILs** (32/32 PASS, $0.00 spread;
3 sentinels N=2 byte-identical). Artifacts: `$AUDIT/v245/distribution.json`,
`V245_WALKFORWARD_RESULTS.md` (auto-generated), per-cell `v245wf_*` dirs.

| regime | n | mean-Δ | p25-Δ | median-Δ | windows Δ<0 | worst / best window Δ |
|---|---:|---:|---:|---:|---:|---|
| crisis | 12 | **+$220** | −$187 | +$31 | 6/12 | −$2,167 (20241205) / +$3,519 (20200101) |
| trend | 10 | −$236 | −$1,291 | −$473 | 7/10 | −$1,641 (20231211) / +$2,884 (20200927) |
| recent | 10 | **−$129** | −$641 | −$297 | 7/10 | −$1,903 (20240723) / +$2,811 (20260228) |
| pooled | 32 | −$31 | **−$716** | −$162 | 20/32 | spread $5,686 |

Falsifier clauses: **recent conjunction FIRED** (mean-Δ −$129 < +$100 AND
p25-Δ −$641 < +$400); **pooled p25-Δ FIRED** (−$716 < $0); trend (−$236) and
crisis (+$220) pass their −$300 floors.

Nuance worth recording (not a verdict changer): at the LEVEL (not Δ)
distribution, gdelt_solo tightens crisis p25 (−$638 vs baseline −$1,089) and
trend p25 (−$137 vs −$572) — the signal compresses some left-tail windows while
taxing the median window (Δ median negative in all three regimes, 20/32 windows
negative). Same family shape as V238's series-ON and V240.B's whale_flow:
information that reshuffles exposure toward crisis at a recent/trend median tax,
pooled ≈ flat (−$31) with variance ($5.7k Δ spread).

## Verdict

**REFUTED — `gdelt_solo` stays FLAG-GATED OFF.** Two pre-registered clauses
fired; ADOPT would also have failed its pooled mean-Δ > $0 requirement (−$31).
Zero code shipped (measurement cell only; the V240.C wiring stays, default
inert). Standing baseline unchanged: V240 selective — crisis +$599 /
trend +$2,997 / recent +$30.

Consecutive-refutation ledger post-REFLECTION_V241: 2 (V244 at $0, V245 at one
32-cell grid). Not the same subsystem (portfolio-corr vs info-feed), not the
same window; no skill reflection trigger fires yet — but a third consecutive
refutation WILL fire the stagnation trigger, and the next reflection should
weigh the recurring pattern: **five info-class/conditioning bets since V236
(chop throttle, β-residual, series-ON, whale-gate, gdelt) have all been
pooled-flat-with-variance.** The composite's entry side appears saturated at
daily-bar granularity.

## Next steps

- **V246 (recommended): exit-side adaptivity** — the last structurally
  untouched dimension (REFLECTION_V241 §5; entry/selection/sizing consumed
  40+ versions; exits never touched). A $0 pre-work scorer exists naturally:
  the ledgers carry `mae/mfe/win_capture/loss_capture` columns — score
  counterfactual exit rules (regime-scaled hold, MFE profit-lock, MAE stop)
  on existing V240 confirm trades BEFORE any implementation, exactly the
  V244 protocol.
- {dxy, yield_curve} subset cell: still queued, but V245's result lowers its
  prior (5th and 6th info-feed bets are the same family).
- V243-A blacklist extension re-score once the parallel Opus track lands.
