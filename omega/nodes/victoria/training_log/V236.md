# V236 — ship the banked trend-IC overlay, regime-gated (PRE-REGISTERED, conditional)

**Date:** 2026-07-02 (pre-registered during V235; execution conditional on V235's verdict)
**Author:** claude (Fable 5)
**Parent:** V235 (walk-forward distributional eval — instrument-only)
**Status:** PRE-REGISTERED — **do not execute until V235's walk-forward trend
distribution is in** (`training_log/V235_WALKFORWARD_RESULTS.md`).

## Hypothesis (one sentence)

The banked V229 drawdown-gated trend-IC overlay (+$1,428 hermetic on the single
2023q4 window; trend edge replicated 3× across V222/V224/V229) generalizes across
the walk-forward trend distribution, and shipping it default-ON **gated on the
runtime trend regime** produces the campaign's first surviving shipped alpha.

## Ship condition (pre-committed, measured by V235 — NOT re-negotiable after seeing data)

SHIP iff, on the V235 walk-forward trend-labeled windows (N ≥ 5):

- **mean-Δ_trend > +$500** (Δ = trendic − main per window), AND
- **min-Δ_trend > −$200** (no trend window pays more than $200 for the overlay).

Both bars come from V235's pre-registration. If either fails → the bank stays
banked, V236 is logged REFUTED-BY-DISTRIBUTION with zero code shipped, and the
V229 entry in the bank is annotated with the distributional read.

Additional non-regression reads (report, not gate — the overlay is regime-gated
at runtime so crisis/recent exposure should be ~nil):
- mean-Δ_crisis and mean-Δ_recent from the same grid, for the gating leak check.

## Planned change (only if SHIP)

- Flip the V229 stack flags default-ON **behind a runtime trend-regime gate**:
  `ic_seed_weighting`, `regime_conditional_ic_weighting`,
  `ic_drawdown_gate_enabled` (threshold 0.12), seeded pooled ICs first
  (V218.B inertness trap), OMEGA_R3_ICS semantics moved into config.
- Regime gate: overlay active only when the runtime regime classifier reads
  trend/normal-trending; crisis and recent/chop stay equal-weight (the V229
  measurement showed the overlay loses crisis/recent un-gated).
- Files: `omega/nodes/victoria/strategy.py` (flag defaults + regime gate),
  `scripts/run_training.py` (seed loader default). One bet, ≤3 files.
- `assert_cell_identity` gains an `--expect-trendgate` claim (S effort) so the
  ship cell proves the gate wiring live (the V213 SILENTLY-INERT class).

## Falsifier for the shipped state

After shipping, the standing main IS the shipped config. Any subsequent
walk-forward re-run where trend mean-Δ (shipped vs pre-ship main) ≤ $0 across
≥5 trend windows ⇒ revert the default and log the refutation. Single-window
regressions do NOT trigger reverts (that's the old Goodhart loop).

## Determinism pre-reqs

Standard arc: `check_no_wallclock` + `check_frozen_http_fence` +
`assert_cell_identity` per cell; N=2 sentinel on at least one trend window at
the same sleep as the V235 grid; any FAIL → bisect (`per_field_diff` /
`trade_field_diff`), fsum-fence, re-run.
