# V248 — regime-conditional exit parameters (first bet under the V247 ruler)

**Date:** 2026-07-13
**Author:** claude (Fable 5)
**Parent:** V246 (mechanism lineage) / V247 (measurement lineage — this is the
first pre-registration whose bars are derived from `V247_RULER.md` +
`V247_RULER_CANDIDATES.md` β structure)
**Status:** REFUTED_AT_SCORING (2026-07-13) — best cell +$494 pooled < +$625
bar; grid not run, $0 spent

## Phase 3 candidate selection (recorded before any V248 result exists)

From {V246 exit adaptivity, V243-A blacklist ext}, per the V247 mandate:

- **V243-A rejected.** It is a selection-changing (high-coupling) mechanism;
  its measured pooled 2·SE is $1,409 and its known, deterministic pooled
  mean-Δ (+$1,197) sits BELOW its class-honest bar. A re-run is byte-identical
  — zero new information — and adopting it would require lowering the bar
  after seeing the number, which the anti-Goodhart guard forbids. It stays
  refuted; universe work re-enters only with a mechanism that changes its
  coupling class or after the manifest widens (V247 Phase 1 α sub-path 1).
- **V246 exact global config rejected as a re-run.** Byte-deterministic:
  pooled +$627 vs a $625 bar forever, by $2. No new information either.
- **CHOSEN: regime-conditional exit parameters** — REFLECTION_V246 §5
  priority #1. New bet, same dormant tested engine surface, LOW-coupling
  class (exit-only; V246 measured pooled Δ-sd $1,767 → 2·SE $625 — the one
  class this instrument resolves).

## Hypothesis

V246 proved a single global (trail_keep, hold_win) pair moves pooled PnL
≈ +$627 but is forced to compromise across runtime regimes. The OFF ledgers
show the three runtime-regime trade pools are wildly different (normal: 241
trades, −$886 total; high_vol: 38, +$6,764; crisis: 64, +$31,570) and V246's
grid showed regime-heterogeneous deltas. Bet: letting each RUNTIME regime
(`normal` / `high_vol` / `crisis` — the causal, non-lookahead label) carry its
own (trail_keep_frac, max_hold_win, max_hold_lose) recovers the compromise
cost, clearing the pooled bar with room instead of by $2.

**Coupling-class declaration (required by V247_RULER.md §7):** exit-only /
low-coupling. Applicable ruler row: pooled 2·SE ≈ $625, recent SE ≈ $360.

## Mechanism

- Engine already parametrized (V246, dormant). New flag
  `exit_regime_adaptivity_enabled` + 9 per-regime fields (defaults = legacy
  literals 0.5/10/6 → OFF and unknown-regime behavior byte-identical).
- `run_training.py` pushes the current runtime regime into the engine once
  per cycle AFTER the cycle completes → marks at cycle t use regime(t−1)
  (1-cycle lag, strictly causal; cycle 1 uses `normal`).
- $0 scorer (`scripts/v248_exit_scorer.py`, extends the validated v2 exact
  bar-mark replay): replays the committed OFF ledgers with the SAME lag,
  reading the per-cycle regime series from each window's committed
  `signal_fingerprint.jsonl`. Per-regime OFAT sweep around the incumbent:
  trail_keep ∈ {0.25, 0.75}, hold_win ∈ {8, 14}, hold_lose ∈ {4} per regime,
  plus best-per-regime composition cells. Validation gate unchanged: the
  incumbent replay must reproduce ≥95% of (exit_cycle, pnl).
- Known scorer blind spot (V246 post-mortem): re-entry capital coupling.
  Mitigation shipped in V247 Phase 2 — the grid aggregator reports the
  open-key re-entry coupling counter, and the scorer's prediction is
  explicitly a point estimate, not a gate substitute.

## Pre-registered gates (β structure — FIXED before any result exists)

**Separator/scorer gate (no grid unless):** some per-regime combo predicts
pooled mean-Δ ≥ +$625 AND recent mean-Δ ≥ −$360 AND pooled level-p25 not
worsened. Otherwise verdict = REFUTED_AT_SCORING, $0 spent.

**Grid verdict — ADOPT iff ALL of:**
1. pooled mean-Δ ≥ **+$625** (the low-coupling-class 2·SE from V247_RULER.md)
2. seeded-bootstrap 95% CI on pooled mean-Δ excludes 0
3. recent mean-Δ ≥ **−$360** (no-regression floor, ≈1·SE one-sided; rejects a
   true −$900 recent regression with ~93% power)
4. **Gated tail metric (pre-named per V247 #1): LEVEL-p25.** No regime's
   level-p25 may worsen by more than **$250** vs OFF. Δ-p25 is REPORTED
   (dual-tail) but NOT gated — V245/V246 showed Δ-p25 penalizes reshuffling
   that tightens the actual outcome tail.
5. 0 determinism FAILs; OFF arm byte-identical to the V240 confirm cells
   (OFF cells are reused, not re-run — same protocol as V245/V246).

**Falsifier:** any of 1–4 fails → REFUTED under the V247 bars; flag stays
OFF; no bar renegotiation after results. If pooled lands in [+$400, +$625)
the result is logged as "inside noise, no adopt" — NOT as a near-miss queue
item (that queue pattern is what V247 existed to kill).

## Changes

- Files: `omega/core/paper_trading.py` (regime-conditional param lookup +
  setter), `omega/nodes/victoria/features.py` (flag + 9 fields),
  `scripts/run_training.py` (per-cycle regime push, flag plumbing),
  `scripts/v248_exit_scorer.py`, `scripts/v248_exit_grid.sh`,
  `scripts/v248_wf_aggregate.py` (imports `scripts/wf_obs.py` — dual-tail +
  re-entry tables are part of the verdict output).
- Flags: `exit_regime_adaptivity_enabled` (default OFF). V246's
  `exit_adaptivity_enabled` stays OFF. Refuted-era flags stay OFF.

## Gate results — REFUTED_AT_SCORING (2026-07-13, claude Opus 4.8)

**Status:** REFUTED_AT_SCORING. The $0 separator gate did not pass, so per the
pre-registered rule ("no grid unless some cell passes") the 32-cell grid was
**not run — $0 spent.** This is the V247 ruler working as designed: it caught a
near-miss before any grid compute.

**Scorer** (`scripts/v248_exit_scorer.py`, artifact
`$AUDIT/v248/scorer.json`) — per-regime OFAT sweep + best-per-regime
composition, Δ vs the replayed incumbent (0.5/10/6 all regimes):

| cell | moved% | poolΔmean | poolΔp25 | recΔmean | trnΔmean | crsΔmean | gate |
|------|-------:|----------:|---------:|---------:|---------:|---------:|:----:|
| normal:trail_k=0.25 | 13.1 | 91 | 34 | 45 | 180 | 56 | – |
| normal:trail_k=0.75 | 14.6 | −123 | 166 | 88 | −864 | 319 | – |
| normal:hold_win=8 | 8.5 | −28 | −53 | −54 | 69 | −86 | – |
| normal:hold_win=14 | 7.3 | −40 | −144 | 30 | −81 | −64 | – |
| normal:hold_lose=4 | 2.0 | 9 | 0 | −1 | 6 | 19 | – |
| high_vol:trail_k=0.25 | 2.9 | 37 | −8 | 57 | 11 | 43 | – |
| high_vol:trail_k=0.75 | 3.8 | 0 | −71 | 9 | 131 | −117 | – |
| high_vol:hold_win=8 | 4.4 | 63 | 0 | 81 | 6 | 95 | – |
| high_vol:hold_win=14 | 2.0 | −17 | 0 | 6 | −11 | −40 | – |
| high_vol:hold_lose=4 | 0.9 | 0 | 0 | 0 | 0 | 0 | – |
| crisis:trail_k=0.25 | 4.4 | 183 | −2 | 8 | 668 | −76 | – |
| crisis:trail_k=0.75 | 7.0 | −234 | 112 | −22 | −865 | 116 | – |
| crisis:hold_win=8 | 5.2 | −125 | 123 | −13 | −443 | 47 | – |
| crisis:hold_win=14 | 5.0 | −108 | 0 | 112 | −367 | −74 | – |
| crisis:hold_lose=4 | 0.9 | 0 | 0 | 0 | 0 | 0 | – |
| **combo** (n:trail_k=0.25 + hv:hold_win=8 + cr:trail_k=0.25) | 20.1 | **494** | 285 | 61 | 1395 | 103 | – |

**The best cell (composition) predicts pooled mean-Δ = +$494** — short of the
+$625 low-coupling 2·SE bar by $131. It satisfies the OTHER two gate legs
(recent Δ +$61 ≥ −$360; pooled level-p25 Δ +$285 ≥ 0), but the pooled-mean bar
is binding and unmet. +$494 lands squarely in the pre-registered
**[+$400, +$625) "inside noise, no adopt"** band — logged as such, NOT queued
as a near-miss (that queue pattern is exactly what V247 exists to kill).

**Instrument note (transparent).** The incumbent replay reproduces exit-cycle
at 99.1% but exact-pnl at 91.5% (< the 95% validation gate), so the scorer's
own `verdict` field reads `INVALID_REPLAY`. This 91.5% floor is **intrinsic to
the replay class, not a V248 regression** — the committed `v246_exit_scorer_v2`
returns byte-identical 99.1% / 91.5% on the same V240-confirm ledgers (sub-$20
mark-rounding on trades the candidate does not move). Crucially, the DECISION
metric is the Δ, and Δ is robust to that floor: the best cell moves only 20.1%
of trades, so ~80% of trades replay identically on both arms and contribute
**exactly $0** to the delta — the rounding error lives almost entirely in the
unmoved trades, which cancel. The +$494 reading is therefore trustworthy for
the adjudication; refining exact-pnl fidelity cannot plausibly close a $131
pooled gap that already excludes the unmoved-trade rounding.

## Conclusion

**V248 REFUTED_AT_SCORING — regime-conditional exit parameters do not clear the
V247 low-coupling bar.** The compromise cost V246 identified is real
(regime-heterogeneous deltas, e.g. crisis:trail_k=0.25 → trend +$668, and the
composition → pooled +$494) but recovering it does not lift pooled mean-Δ to
the +$625 required to beat the instrument's noise. This is the fourth
consecutive exit/portfolio refutation (V244 corr-spike, V245 gdelt, V246 exit
global, V248 exit regime-conditional) and the FIRST adjudicated entirely under
the V247 ruler — the ruler spent $0 of grid compute to reach it, versus the
full 32-cell grid V246 spent to reach an equivalent +$627≈2·SE near-miss.

Flag `exit_regime_adaptivity_enabled` stays **OFF**. The engine parametrization
is retained **dormant** (byte-identical baseline, OFF verified: default flag
False, defaults = legacy 0.5/10/6, `regime_exit_params=None` ⇒ pre-V248 code
path; `set_current_regime` inert without params). Same posture as V246's dormant
`exit_adaptivity_enabled` — a future re-derivation under a widened window
manifest (V247 Phase 1 α sub-path 1, which lifts the low-coupling 2·SE bar by
adding recent windows) could re-test this surface without new engine work.

## Next steps

1. **Exit-side mechanism space is saturated under the current manifest.** Both
   the global (V246) and regime-conditional (V248) exit bets land in the
   $494–$627 near-miss band — below bar, above noise-floor-only. Per V247, do
   NOT keep grinding OFAT variants of exits; that is the near-miss queue V247
   killed. The lever to change the verdict is the RULER, not the mechanism:
   pursue **V247 Phase 1 α sub-path 1** (widen the walk-forward window manifest
   via `walk_forward_freeze` to add independent recent windows), which lowers
   the 2·SE bar and could re-qualify the +$494 combo — but only after the bar
   is re-derived pre-registration on the wider manifest.
2. Entry-side info feeds (V240.B–V245) and portfolio-corr (V242–V244) remain
   CLOSED. Universe work (V243-A) re-enters only with a coupling-class change
   or a widened manifest.
3. Refuted-era flags (V241 reasoning, V244 corr-spike, V245 gdelt, V246+V248
   exit) all stay OFF.
