# V197 — Observability reset (PipelineTracer + strategy_selector bug)

**Date:** ~2026-05-24
**Parent:** V195-V196 (stripped preset + strong-signal bypass)
**Status:** complete (infra, not signal change)
**Commits:** `42c7ede feat(observability): V197 PipelineTracer foundation + orchestrator wi...`, `8d1b773 feat(observability): wire PipelineTracer at strategy + paper_trading ...`, `fbef763 feat(observability): complete PipelineTracer wiring (6/6 boundaries)`, `b33143c diag(victoria): V197 signal-routing assertion at strategy entry`

## Hypothesis

V194-V196 kept producing 0-trade live runs and we couldn't tell
whether signals were absent, gated, or silently overridden. Need
end-to-end tracing of: signal → composite → sub-strategy vote →
ensemble → conviction filter → proposal → paper_trade.

## Changes

- `PipelineTracer` foundation wired through orchestrator
- 6/6 pipeline boundaries instrumented:
  signal_gen → composite → sub-strategy → ensemble → conviction →
  paper_trade
- Hard assertion at strategy entry to catch routing bugs
- Caught and documented the `strategy_selector` silent-override
  trap (V195b → V196 `preset_override_mode`)
- Post-mortem: `docs/research/...post-mortem.md` (commits `35c7ec7`,
  `c93a63f`)

## Gate results

| Gate              | PnL | Trades | WR | PF |
|-------------------|----:|-------:|---:|---:|
| v197_diagnostic   | $0  | 0      | —  | —  |

Diagnostic run, no signal change. Purpose: validate tracer coverage.

## Conclusion

✅ **Critical infra**: we can now answer "why didn't this trade
fire?" with a trace rather than a guess. This unblocks all future
debugging.
✅ Caught a real bug: the diagnostic-log "composites={}" artifact
that previously suggested a routing issue was confirmed to be a
log-format artifact, not real data loss (commit `c93a63f`).

## Next steps

→ **V198**: monitor-only, no new signal change — let the tracer
accumulate baseline data.
→ **V199**: with tracing in place, ship the carry-only sub-strategy
(see V191 next-steps) and verify via tracer that carry signals reach
the composite even when other sub-strategies abstain.
