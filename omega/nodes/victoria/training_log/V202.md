# V202 — Remove crisis size amplifier + restore crisis half-Kelly

**Date:** 2026-05-29
**Author:** claude
**Parent:** V201 (threshold-discount removal — trend confirmed, crisis falsifier triggered)
**Status:** running

## Hypothesis

V201 isolated `crisis_short_bias` as the cause of the V172→V199 trend
regression by neutralising the threshold discount (recovered ~60% of
the trend gap). Crisis was barely changed (−$18,996 vs −$19,410),
which **falsified the selection theory** and points the next bet at
sizing: in `crisis`/`high_vol` cycles V201 still amplifies shorts
×1.3, scales longs ×0.5, and skips the crisis half-Kelly 0.5× safety
net. Snapback rallies on crisis snaps then squeeze oversized loose
shorts.

**V202's bet (one crisis-sizing bet, bundled):** stop amplifying short
size in crisis cycles and let Kelly cap risk normally.

1. Drop the post-weight size block at `strategy.py:2814-2828` (shorts
   ×1.3, longs ×0.5).
2. Restore the crisis half-Kelly 0.5× safety net at
   `strategy.py:2842-2851` by removing the
   `and not self.features.crisis_short_bias` guard so crisis cycles
   always get the 0.5× scaling regardless of the feature flag.

These are two edits but the same hypothesis ("crisis sizing is too
aggressive"). Bundling is intentional. **If results are ambiguous
(e.g. crisis improves but recent regresses), V203 splits this into
V203a (size-amp removal alone) + V203b (kelly restore alone) to
attribute which half mattered.** If crisis stays < −$15K both edits
were ineffective and we move to exits.

## Changes

- Files touched: `omega/nodes/victoria/strategy.py` (two edits at the
  two adjacent crisis-sizing blocks).
- Configs / preset name: none.
- Feature flags toggled: none. `crisis_short_bias=True` stays on.
- Anything reverted vs parent: V201's threshold-discount removal is
  preserved (it shipped the trend recovery).

## Falsifiers (pre-registered)

- **Crisis (primary):** must clear **−$10K** to confirm sizing is the
  lever. If crisis stays below −$15K, sizing isn't the binding
  constraint either and V203 = exit-side trail rework.
- **Recent:** if PnL drops below V199's **+$2,478**, the size
  amplifier was *material to recent*. V203 → regime-conditional
  sizing (size-amp on for the recent NEAR cluster, off for crisis
  snapback rallies).
- **Trend:** ≥ +$10K maintained (V201 baseline +$12,996). A drop
  here means the size amp was also helping trend shorts and we
  over-corrected.
- **Trade count floor:** any gate below 20 trades fails the hard
  gate.
- **Attribution split flag:** if crisis improves AND recent
  regresses, V203 splits the bundle. If both improve, ship as-is.

## Gate results

| Gate    | PnL | Trades | WR | PF | Max DD | Notes |
|---------|----:|-------:|---:|---:|-------:|-------|
| recent  |     |        |    |    |        |       |
| trend   |     |        |    |    |        |       |
| crisis  |     |        |    |    |        |       |

Result files: `data/v202_<gate>_results.json`, `data/v202_<gate>_trades.csv`.
Gate-pass report: `data/v202_<gate>_gate_result.json`.

## Conclusion

_To be filled in after gates land._

## Next steps

→ **V203**: branches on outcome — split-attribution (if ambiguous),
regime-conditional sizing (if recent regressed), or crisis exit-side
trail rework (if crisis still < −$15K).

## References

- V201.md — parent. Threshold-discount diagnosis, crisis falsifier.
- V172.md — trend high-water (+$18,437).
- V199.md — recent high-water (+$2,478).
- `strategy.py:2814-2828` — size amplifier block (this version
  removes).
- `strategy.py:2842-2851` — crisis half-Kelly skip (this version
  un-guards).
