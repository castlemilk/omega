# V234 — break the 2024aug deadband at the sizing/exit layer (post-REFLECTION_V233 pivot)

**Date:** 2026-06-29
**Author:** claude
**Parent:** V233 (application-SITE fix — **REFUTED, falsifier branch 4**: 2024aug Δ == $0.00
under pre-demean AND weight ≤ 0.6; common-mode re-injection moved it only +$6.31 with 0 trades
flipped and a 2020q1 regression; pre-demean actively HURT 2022h1 −$4.5k→−$4.9k).
**Status:** **REFUTED — falsifier branch 3 (pre-grid).** A pre-grid forensic showed the
throttle is inert at thresh 0.12 (traded-candidate `_skew_dd_mag` maxes at 0.0644 << 0.12 →
throttled=0 → grid guaranteed Δ==$0.00) AND that the V227 drawdown gate does not select the
2024aug traded losers (realized drawdown is a lagging entry signal; the losers are
pre-reversal shorts with ~0 pre-entry drawdown). Wiring is fine (`_skew_dd_mag` propagates,
key_present=100%) — the session-entry "not propagated" hypothesis is disproved. Grid NOT
launched (burn saved). See `REFLECTION_V234.md`; V235 pivots to candidate-selection. The
default-inert throttle code is retained for V235 reuse with a better gate.

## Why this version exists (read `REFLECTION_V233.md` first)

Seven consecutive additive-term-at-the-composite interventions (V227 skew, V231 instrument,
V232 brake, V233 ×3 sites/weights + common-mode) have left the worst crisis window
(snap_crisis_2024aug, −$9,508) **byte-identical at Δ == $0.00**. The reflection establishes
the mechanism: the V227 drawdown gate *fires correctly* on 2024aug, the term IS computed and
added, but after `×_cs_norm` into `w_conv` (`strategy.py:~1922`) the residual is smaller than
the gap to any conviction floor/threshold (`strategy.py:1589-1614`), so **no trade flips**.
The deadband lives **downstream of the composite**, in the conviction→trade translation. V234
acts there. **The site/signal-additive subsystem is closed for 2024aug** (per the reflection);
do NOT propose another composite-additive term.

## Hypothesis

The crisis edge is destroyed at the **sizing/exit layer**, not the conviction layer. On a
broad correlated grind-down the model holds correctly-identified losers at full size because
the crisis signal can only nudge *conviction* (which never crosses a boundary), never *size or
hold*. If we let the **already-correctly-firing V227 drawdown gate** act directly on **position
size and exit timing** — throttle size and tighten the cut when realized drawdown is extreme —
the 2024aug trade ledger will *actually change* (Δtrades ≠ 0 / Δsize ≠ 0), which no
composite-side change ever achieved, and the loss will shrink.

## Changes (planned, all default-inert behind flags)

- **Crisis size throttle** — when the V227 drawdown gate fires (drawdown > 0.12), scale the
  position-size multiplier down (sweep throttle ∈ {0.5, 0.25, 0.0}). Site:
  `conviction_size_multiplier` / the size path (`strategy.py:283` and the sizing call site).
  Flag: `crisis_size_throttle_enabled` + `crisis_size_throttle` (float).
- **Crisis exit tighten** — when the gate fires and a position is in drawdown, shorten the
  hold / tighten the stop (the exit path that sets `hold_cycles` / stop). Flag:
  `crisis_exit_tighten_enabled`.
- Both reuse the **existing** V227 per-ticker drawdown-AND-gate (no new signal). Files:
  `omega/nodes/victoria/strategy.py`, `features.py` (flags). `math.fsum`-fenced; flags
  default-inert so OFF arms reproduce the standing main byte-for-byte.

## Grid plan

- **Reuse baselines (NOT re-run):** the V227-skew standing main per window
  (2020q1 +15,503.01 / 2022h1 −2,991.17 / 2024aug −9,507.88) from `v232_dist`.
- **NEW cells (crisis, sizing/exit configs × 3 windows), N=2 hermetic** on gamma:
  `OMEGA_AUDIT_OUTPUT_DIR=/Volumes/gamma-systems-2/omega-victoria-data N=2 GATES=crisis`.
- Aggregate with the V233 harness shape (`Δ = pnl − baseline`; binding read = 2024aug Δ).
- **Conditional trend/recent leak spot-check** — only if a crisis config moves 2024aug
  loss-reducing without regressing 2020q1/2022h1.

## Falsifier (pre-committed)

1. **A sizing/exit config changes the 2024aug ledger (Δtrades ≠ 0 OR Δsize ≠ 0) AND reduces
   the 2024aug loss AND does not regress 2020q1/2022h1** → the deadband was at sizing/exit;
   SHIP it crisis-gated; V235 carries the deferred xsec-corr signal through it.
2. **It reduces 2024aug but regresses a non-binding window** → window-conditional; keep
   crisis-gated, V235 finds the right gate shape.
3. **2024aug STILL returns Δ == $0.00 (ledger unchanged) even with size→0.0 on a fired gate**
   → the gate is not selecting the losing positions on 2024aug at all (selection problem, not
   sizing) → V235 pivots to the candidate-selection layer; escalate the reflection (the entire
   V227 drawdown-gate *selection* is suspect on this window).
4. **Determinism FAILs** → new FP/ordering channel from the size/exit arithmetic; bisect with
   `per_field_diff.py` / `trade_field_diff.py`, fsum-fence, retry.

## Determinism pre-reqs (carry the arc)
`check_no_wallclock.py` + `check_frozen_http_fence.py` + `assert_cell_identity.py` PASS per
cell. Size/exit math `math.fsum`-fenced and per-ticker independent (V221 discipline). New flags
default-inert; `assert_cell_identity` verifies the flag is set as the cell claims.

## Deferred (→ V235+)
- **Track B #2** cross-sectional correlation-spike signal — re-opens only on a sizing/exit win.
- **B2 trend-IC** ≥3-window trend distribution; **recent 2025** distribution (MATIC→POL fork).
