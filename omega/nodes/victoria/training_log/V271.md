# V271 — Funding-carry live-paper lane at k=1, re-scoped as OOS reconciliation

**Status:** PRE-REGISTRATION (committed before any lane activation)
**Date:** 2026-08-18
**Parent:** [`V268.md`](V268.md) → [`V268_SOAK_FEASIBILITY_VERDICT.md`](V268_SOAK_FEASIBILITY_VERDICT.md) option **(a)**
**Standing baseline (MUST NOT MOVE):** crisis +$599 / trend +$2,997 / recent +$30

---

## 0. What this version is, and what it explicitly is NOT

V268 refuted the *scaled soak* on two independent legs. V271 takes V268 §5
option (a) and nothing else:

- **NOT** a scaled soak. V268 F2 showed Sharpe is bit-identical at every `k`
  (no impact model ⇒ every ratio statistic cancels `k`). The lane runs at
  **k = 1** and the word "scaled" is retired.
- **NOT** an adjudication of V267 G3. V268 F1 showed the high-ADV-tercile
  `recent` CI needs 138 more trades = **6.4–19.4 years**. That objective is
  abandoned here, not deferred.
- **IS** a V251-class **reconciliation**: does the campaign's one confirmed
  alpha, run forward against real Binance perp data at the ledger's own scale,
  reproduce the frozen ledger's regime mix and per-trade edge distribution?

## 1. Pre-registered question

> Does live funding-carry, running forward against real Binance perp data at
> k=1 notional (~$2k median trade, matching V255.C's ledger scale), reproduce
> the frozen ledger's regime mix and per-trade edge distribution — in a
> V251-class OOS reconciliation sense?

## 2. Gates (pre-registered, bars fixed before any observation)

| Gate | Statistic | Bar |
|---|---|---|
| **G1** regime-mix reproduction | KS test, live classified-regime histogram vs V255.C frozen regime distribution | **p > 0.05** |
| **G2** per-trade edge distribution | live median net PnL/trade, distribution-free CI95 (order statistics) | **overlaps V255.D's [$1.13, $2.80]** |
| **G3** execution-cost sanity | realized half-spread on live fills vs V270's 0.4650 bps | **within 2× → [0.23, 0.93] bps** |
| **G-KILL** | paper drawdown on any observation window vs V267's slippage budget | **> 11.83 bps ⇒ halt the LANE (not the daemon), surface** |

## 3. G-MEAS — precommitted measurement-complete definition

**Measurement is complete at the FIRST of:**

- **N = 100 closed funding-carry paper trades**, or
- **18 calendar months** from lane activation.

Rationale for N=100: at the frozen ledger's last-12m arrival rate of
**77.1 trades/yr** (full universe, all terciles), N=100 ≈ **1.30 years**, which
sits inside the 18-month calendar cap. **No earlier re-measurement is
permitted.** Interim reads are operational health only (lane alive, writing
cleanly) and MUST NOT be scored against G1/G2/G3.

## 4. Phase 0 — lane-feasibility audit (blocking, runs before activation)

Standing rule since V234: **no activation until a probe shows the thing can
actually be done as specified.** Phase 0 asks one question:

> **F0 — Can the V253 harness host an additive k=1 funding-carry lane without
> modifying strategy code in `omega/nodes/`?**

**F0 passes** only if BOTH hold:

- **F0a — online-computable entry rule.** Every input to the V255.C/D entry
  decision is computable from data available at or before the decision date.
  Falsifier: recompute each gating input causally (expanding window) and
  compare to the frozen full-span value; **> 5% of dates flipping the
  trade/no-trade decision ⇒ FAIL**.
- **F0b — additive harness seam.** A second lane can be composed into the
  running daemon without editing `omega/nodes/**` and without disturbing the
  existing forward cycle's checkpoint / PnL curve.

**If F0 fails ⇒ report R5 (structural payload), activate nothing.** Per the
operator brief's anti-Goodhart clause, `omega/nodes/**` MUST NOT be modified to
make the lane fit — that is a separate version with its own pre-registration.

## 5. Files this version may touch

- `omega/nodes/victoria/training_log/V271*.md` (this doc + verdict)
- `scripts/v271_lane_feasibility.py` (Phase-0 scorer, $0, read-only)
- **On F0 PASS only:** `omega/live_paper/` (runner-layer lane composition)
- **Never:** `omega/nodes/**`, any strategy parameter, the standing baseline.

## 6. Operational guardrails

- **Live PAPER only.** No broker, no orders, no funds. Fills simulated per the
  V253 harness contract.
- Lane must be **additive** to the running daemon `com.omega.live_paper`
  (**PID 13829**, healthy, last tick 2026-08-18T02:55:00Z, equity $98,439.41).
  Do not restart if avoidable; if a restart is required it is documented in the
  verdict with the PID→PID transition and confirmation that other lanes resumed.
- **Do not touch** the depth collector `com.omega.depth_collector` (PID 76450).
- `harness/.env` stays gitignored.
- **Staging discipline (V270 footgun):** the working tree carries 11 modified +
  31 untracked pre-existing files that are NOT this version's. Stage with
  **explicit paths only** — never `git add -u`, never `git add .`, never a bare
  `--amend` after a broad add.

---

**Pre-registration committed before any lane activation and before any gate was
scored.**
