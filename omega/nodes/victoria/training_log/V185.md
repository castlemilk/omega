# V185 — Microstructure signals (VPIN + Kyle's λ + multi-level OFI)

**Date:** ~2026-05
**Parent:** V184 (stacked exit-trail wiring)
**Status:** complete (snapshot strong, live anemic)
**Commits:** `3466693 feat(victoria): V182-V185 — exit trail wiring + microstructure signals`

## Hypothesis

Volatility-Synchronized Probability of Informed Trading (VPIN),
Kyle's lambda, and multi-level order-flow imbalance from L2 book
data will detect informed flow during the flat-vol periods where
breakout/momentum signals go silent.

## Changes

- `VPIN` (volume-bucketed informed-trade probability)
- `kyles_lambda_signal` (price impact per unit volume)
- `lob_features` — multi-level OFI + arrival rate + adverse selection
- Exit-trail wiring from V182-V184 carried in
- All gated on WS-feed health

## Gate results

| Gate              | PnL    | Trades | WR  | PF   | Notes                            |
|-------------------|-------:|-------:|----:|-----:|----------------------------------|
| recent (live)     | $0     | 0      | —   | —    | `v185_vpin_live` — no trades fired |
| snapshot replay   | +$330  | 12     | —   | 6.26 | (per signal-effectiveness review) |

Result files: `data/v185_vpin_live_results.json`,
`data/v185_phase_a_0508_v185_signal_contribs.jsonl`.

## Conclusion

⚠️ Mixed. Snapshot replay shows VPIN is the strongest microstructure
signal we have when conditions align (PF 6.26). But **live runs need
sustained WS-feed accumulation** to compute meaningful VPIN buckets
and λ — running for 200 cycles in a flat tape produces too few
trades for the signal to be visible.

Lesson: WS-only backtest replay is misleading; the signal needs
real-time cycle accumulation, not historical bars.

## Next steps

→ **V186**: LLM tie-breaker + risk-scaling — try to convert
abstain → trade decisions when conviction is borderline.
→ **V187**: dynamic correlation-graph signal (EMGNN-lite).

Parking lot: revisit VPIN/Kyle once live WS uptime > 24h.
