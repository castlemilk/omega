# V148 — Pre-loop baseline

**Date:** ~2026-04 (last commit on `main` before worktree iteration)
**Parent:** V147
**Status:** complete (shipped to main as `f2cff01`)
**Commit:** `f2cff01 feat(victoria): V148 best-of-phases — meta_learner_exit_only + continuous_sizing`

## Hypothesis

Combine the best individual phases from V145–V147 (meta-learner used
only for exits, continuous sizing on conviction) into a single
preset.

## Changes

- `meta_learner` engaged only on exit decisions, not entries
- Continuous (vs discrete) position sizing tied to conviction score
- Carryover from V145 `openai_compatible` LLM provider
- Confidence-surface gate applied to long entries (V143 fix)

## Gate results

Snapshot results: `data/bt_v148_<gate>_*.json` (benchmark dir was
last refreshed at `bt_v139_*`; V148 ran on the older benchmark
harness).

## Conclusion

This was the last "main-ship" version before the long V149–V198+
iteration sequence moved to worktree `claude/inspiring-edison-31c334`.
Solid baseline but heavily trend-biased; crisis still unsolved.

## Next steps

→ **V149+**: move to worktree, start IC-weighted ensemble exploration
(eventually leading to V170 per-regime IC).
