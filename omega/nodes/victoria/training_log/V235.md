# V235 — walk-forward distributional eval + universe re-validation (PIVOTED)

**Date:** 2026-07-02 (pivot; original pre-registration 2026-07-01 below)
**Author:** claude (Fable 5)
**Parent:** V234 (REFUTED, branch 3) + DEEP_REVIEW_2026-06_FABLE (authorized by user).
**Status:** ACTIVE — instrument-only version. The original candidate-selection
pre-registration below is **SUPERSEDED, not executed**: its own falsifier branch 3
fires on evidence already in hand (8 consecutive refutations on `snap_crisis_2024aug`;
$25,435 measured cross-window spread; crisis distribution mean already positive).
Per the deep review: stop iterating on the window, make the yardstick able to see.

## V235 (pivoted) pre-registration

**Bet (one sentence):** a regime-tagged walk-forward distribution (≥25 rolling 60-day
windows, 2020→2026, V215 freeze recipe) will change at least one standing verdict —
specifically, recent's +$4,901 (N=1) and/or the banked trend-IC +$1,428 (N=1) will
NOT reproduce as a distribution mean at their single-window magnitude.

**Deliverables:**
1. `training_log/V235_UNIVERSE_REVIEW.md` — blacklist forensic (preflight; done).
2. `scripts/walk_forward_freeze.py` — window builder + regime labeler +
   `data/walk_forward_manifest.json`.
3. `scripts/walk_forward_grid.sh` — manifest-driven grid: per window,
   `main` (V227-skew standing main) and `trendic` (V229 drawdown-gated trend-IC
   stack) cells, sleep=0, N=1 (N=2 determinism sentinels on 3 windows),
   `assert_cell_identity` + `check_no_wallclock` + `check_frozen_http_fence`
   per cell, gamma-redirected, resumable.
4. `training_log/V235_WALKFORWARD_RESULTS.md` — regime-tagged distributions
   (mean/p25/min per regime) for both configs + verdicts.
5. Pre-registrations for V236 (trend-IC ship, conditional) and V237
   (portfolio-level crisis cap); SKILL.md loop-process updates (Goodhart
   tripwire, walk-forward requirement, universe re-validation trigger).

**Falsifiers / decision rules (pre-committed):**
- **Trend-IC ship gate (feeds V236):** ship default-ON (trend-regime-gated) iff
  walk-forward `mean-Δ_trend > +$500` AND `min-Δ_trend > −$200` (Δ = trendic − main
  per trend-labeled window, N≥5 windows). Otherwise the bank stays banked.
- **Recent reproduction:** if the mean of recent-labeled windows under the standing
  main is materially below the +$4,901 single-window number (< +$1,000 mean or
  negative p25), the "recent carries the system" narrative is refuted → mandatory
  reflection + strategic re-assessment before any V236 ship.
- **Determinism:** sentinel N=2 cells must PASS at sleep=0; any FAIL → bisect
  (per_field_diff / trade_field_diff), fence, re-run — before trusting any N=1 cell.
- **Coverage floor:** ≥25 usable windows, ≥5 per regime bucket for trend/recent;
  crisis may be < 5 (2020–2026 contains a limited number of true crisis windows) —
  report actual counts, no silent caps.

**Not in scope:** any `strategy.py` change; any blacklist flip (V238); any new
crisis intervention (V237 pre-registration only).

---

# [SUPERSEDED] Original pre-registration — the 2024aug candidate-SELECTION layer

**Date:** 2026-07-01
**Author:** claude
**Parent:** V234 (crisis size throttle — **REFUTED, falsifier branch 3**: throttle inert at
thresh 0.12; traded 2024aug candidates max `_skew_dd_mag` = 0.0644 at entry; the V227
drawdown gate selects non-traded names, never the traded losers).
**Status:** superseded by the pivot above (falsifier branch 3 fired pre-work)

## Read REFLECTION_V234 first

The wall is now located precisely. Eight consecutive interventions (V227–V233 additive
composite terms, V234 sizing throttle) all failed on 2024aug because **each keys off a
signal that does not discriminate the 2024aug losing set at entry.** V234's forensic proved
the V227 realized-drawdown gate and the traded losers are **disjoint**: the losers are
**shorts entered before reversals**, and realized past drawdown of a rising/flat asset is
~0. Lagging selectors are structurally blind here. **Do NOT propose another gate/term keyed
on realized drawdown, VRP, or any lagging composite** — that hypothesis class is exhausted
for 2024aug across signal, site, weight, and now sizing.

## Hypothesis

The 2024aug loss is a **selection** failure, not a sizing or conviction-composition failure.
The model enters correctly-identified-in-hindsight *losing* shorts on a broad correlated
grind-down because nothing at the **candidate-selection** layer vetoes a short whose edge is
about to mean-revert. If we add an **entry-time discriminator** — a signal that is
informative at the moment of entry, not lagging — that either (a) vetoes the pre-reversal
shorts or (b) flips/downweights them, the 2024aug **trade ledger will change** (Δtrades ≠ 0,
the bar V234 never cleared) and the loss will shrink, without regressing 2020q1 (+$15.5k) or
2022h1.

## Pre-work (MANDATORY before any code — cheap, ~2 short runs)

Establish *what actually distinguishes the losers from the winners at entry* on 2024aug,
using the same env-gated-probe technique V234 used (instrument the `raw_weights` site in
`strategy.py:~3384`; dump per-candidate `(ticker, side, entry_composite, dd_mag, <candidate
features>, subsequent_pnl)` and correlate). Answer:
1. At entry, which candidate feature *does* separate the eventual losers from winners on
   2024aug? Candidates to log: cross-sectional correlation / basket-beta, funding, short
   crowding, realized-vol term structure, forward return sign vs composite sign.
2. Does ANY existing computed signal already carry that separation (so V235 is a wiring, not
   a new-signal, task)?
3. Confirm the pre-reversal-short story quantitatively: bucket 2024aug shorts by
   entry→exit price change sign; verify the losers are shorts into subsequent price *rises*.

**If the pre-work finds no entry-time separator** among available signals → the loss may be
irreducible with current features on this window (regime is genuinely unpredictable at
entry) → escalate to a meta-reflection on whether 2024aug is a Goodhart target (cf.
[[victoria_snapshot_build_trap]]: crisis Goodhart already quantified at ~$25k cross-window
spread; V227's +$630 did not generalize). Do **not** grid-search a selector that the
pre-work shows cannot separate.

## Changes (planned, all default-inert behind flags)

- **Entry-time short veto / downweight** at the candidate-selection layer
  (`strategy.py` candidate build, ~2770/3088), gated on the separator the pre-work
  identifies. Reuse the retained V234 *actuator* (size scaling at `raw_weights`) if the
  answer is "downweight not veto".
- Flag(s): `crisis_entry_selector_enabled` + selector params. Default OFF ⇒ byte-identical
  standing main.

## Grid plan (only if pre-work finds a separator)

- Reuse V227-skew standing-main baselines per window (2020q1 +15,503.01 / 2022h1 −2,991.17 /
  2024aug −9,507.88) from `v232_dist`.
- NEW cells: selector configs × 3 crisis windows, N=2 hermetic on gamma
  (`OMEGA_AUDIT_OUTPUT_DIR=/Volumes/gamma-systems-2/omega-victoria-data N=2 GATES=crisis`).
- Binding read = 2024aug Δ; success = mean-Δ>0 AND min-Δ>0 across the 3 windows AND
  **Δtrades ≠ 0 on 2024aug** (the ledger must actually change).

## Falsifier (pre-committed)

1. **A selector config changes the 2024aug ledger (Δtrades ≠ 0) AND reduces the 2024aug loss
   AND does not regress 2020q1/2022h1** → selection was the lever; SHIP it crisis-gated.
2. **Reduces 2024aug but regresses a non-binding window** → window-conditional; keep
   crisis-gated, tune the gate shape in V236.
3. **No available signal separates losers from winners at entry (pre-work) OR every selector
   config leaves 2024aug Δ==$0.00 / worse** → 2024aug is likely irreducible with current
   features → **stop iterating on this window**; write a meta-reflection, redirect effort to
   the deferred trend-IC / recent-2025 distributions and to *portfolio-level* crisis risk
   (basket exposure cap) rather than per-name selection.
4. **Determinism FAILs** → new FP/ordering channel from the selection arithmetic; bisect with
   `per_field_diff.py` / `trade_field_diff.py`, `fsum`-fence, retry.

## Determinism pre-reqs (carry the arc)

`check_no_wallclock.py` + `check_frozen_http_fence.py` + `assert_cell_identity.py` PASS per
cell. Selection math per-ticker independent (V221 discipline); any new accumulation
`math.fsum`-fenced. New flags default-inert; `assert_cell_identity` verifies the flag state.

## Deferred (→ V236+)

- **Portfolio-level crisis exposure cap** (basket-beta / gross-exposure ceiling) as an
  alternative to per-name selection if branch 3 fires again.
- **B2 trend-IC** ≥3-window trend distribution; **recent 2025** distribution (MATIC→POL fork).
