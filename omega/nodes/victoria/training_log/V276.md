# V276 — Hygiene: pin `per_regime_ic_weighting` into the arms that rely on it, then flip its default

**Status:** PRE-REGISTERED (this document committed before any code change)
**Date:** 2026-08-26
**Author:** claude
**Parent:** [`V275.md`](V275.md) — §8 item 1, queued verbatim as "V276-hygiene"
**Standing baseline (MUST NOT MOVE):** crisis **+$599** / trend **+$2,997** / recent **+$30**
**Class:** platform-hygiene / eval-integrity. **No strategy mechanism. No new signal. No grid.**

---

## §0 — Numbering collision, resolved

V275 §0 flagged that `V275_DRAFT_crisis_term_rebind.md` also claimed the number
V275, and recommended renumbering it to `V276_DRAFT`. V275 §8 then queued *this*
work under the same number. Both cannot be V276.

**Operator decision (2026-08-26):** this hygiene version takes **V276** — it is the
one actually pre-registered and run. The crisis-term draft, which remains
**unapproved, unrun, and flag-OFF**, is renumbered to
**`V277_DRAFT_crisis_term_rebind.md`**, and its companion test docstring moves with
it. The draft's content is otherwise untouched; renumbering is not approval.

---

## §1 — Hypothesis

V275 flipped `ic_seed_weighting` to `False` and deliberately **declined** to flip
`per_regime_ic_weighting`, because its Phase 0 audit found the premise split: the
second flag is absent from all 44 arms, and in **7 of them it is live and
load-bearing** — including `v274_ic_on_grid.sh:38` and `v274_smoke.sh:32`, the arms
behind V274's own headline result. Flipping it first would have silently redefined
what V274 measured.

The bet:

> Pinning `"per_regime_ic_weighting": true` explicitly into those 7 arms is a
> **no-op that preserves what they measured**. Once pinned, no arm inherits the
> default, so flipping it to `False` is a no-op for every existing arm and a guard
> for every future one — the same structural close V275 achieved for the sibling
> flag, in the only order that does not rewrite history.

**Falsifier (G2):** if any sentinel cell moves by a cent, the pin-then-flip has
surfaced a reliance the audit missed. That is the finding — **STOP and report as
R5**; do not chase the fix in this version.

---

## §2 — Why this flag's safety argument is NOT V275's

This must be stated plainly, because reusing V275's reasoning here would be wrong.

V275's G2 rested on a **structural** claim: `ic_seed_weighting` has *no
strategy-layer consumer* (`tests/test_features_defaults.py::
test_ic_seed_weighting_has_no_strategy_layer_consumer`), so the flip could not
change strategy behaviour by construction.

**`per_regime_ic_weighting` does not have that property.** It IS read at the
strategy layer:

| Site | Code |
|---|---|
| `strategy.py:1135` | `_per_regime = bool(getattr(self.features, "per_regime_ic_weighting", False))` |
| `strategy.py:1205` | `if _per_regime and self._regime_ics:` |
| `strategy.py:1223` | `if _per_regime and self._regime_ics:` |

So the safety argument is **conditional inertness**, not absence of a consumer, and
it rests on the second conjunct:

- `self._regime_ics` is initialised empty (`strategy.py:488`) and has exactly one
  populate path: `update_regime_ics` (`strategy.py:998`), whose only caller is
  `scripts/run_training.py:1155`.
- That call sits **inside the block gated at `run_training.py:1106`** on
  `bool(getattr(_active_features, "ic_seed_weighting", False))` — verified by
  reading the block bounds, not inferred.

Therefore: with `ic_seed_weighting: false`, `_regime_ics` is `{}`, both guards
short-circuit on the empty dict, and `per_regime_ic_weighting` cannot be observed at
any value. With `ic_seed_weighting: true`, `_regime_ics` is populated and the flag
**is** live — which is precisely why the 7 arms must be pinned first.

This is a weaker guarantee than V275's and is recorded as such. G2 is what tests it
empirically.

---

## §3 — Change surface

**Scope: full V275 §8 item 1** (operator-selected 2026-08-26).

*Part A — the pin (7 files, no-op by construction):*

| # | Arm | File:line |
|---|---|---|
| 1 | `R3F` | `scripts/v224_run_grid.sh:37` |
| 2 | `ON` | `scripts/v228_run_grid.sh:15` |
| 3 | `ON` | `scripts/v229_run_grid.sh:18` |
| 4 | `feats` (function-local) | `scripts/v229_xsweep.sh:11` |
| 5 | `TRENDIC` | `scripts/walk_forward_grid.sh:51` |
| 6 | `ARM_ON` | `scripts/v274_ic_on_grid.sh:38` |
| 7 | `ARM_ON` | `scripts/v274_smoke.sh:32` |

Each gains `"per_regime_ic_weighting": true` — the value they already inherit. The
two stale comments (`v274_ic_on_grid.sh:36`, `v274_smoke.sh:7`) stating the flag is
"left at its features.py default" are corrected to say it is now pinned explicitly.

*Part B — the flip (2 files):*

8. `omega/nodes/victoria/features.py:699` — `per_regime_ic_weighting: bool = True` →
   `False`, with the V276 rationale in the existing docstring.
9. `tests/test_features_defaults.py` — replace
   `test_per_regime_ic_weighting_default_is_pinned_true` with an OFF assertion, and
   add a guard test for the §2 mechanism (see §4).

*Part C — the stale prose V275 named but could not touch (4 files):*

Each asserts `ic_seed_weighting` is ON / "the DEFAULT", which V275 made false:

10. `omega/nodes/victoria/strategy.py:1262`
11. `omega/nodes/victoria/signals/crisis_rebind.py:16`
12. `omega/nodes/victoria/features.py:1007`
13. `tests/test_crisis_rebind.py:8`

**Prose only — zero executable lines change in Part C.** The `strategy.py` edit is a
comment; V275 could not make it only because its own brief forbade touching that
file.

*Part D — the renumber (§0):* `V275_DRAFT_crisis_term_rebind.md` →
`V277_DRAFT_crisis_term_rebind.md`, plus its `"""V275: ..."""` docstring in
`tests/test_crisis_rebind.py`.

**Not touched:** `data/standing_baseline.json`, anything else under `data/`, any
executable strategy line, `scripts/run_training.py`.

---

## §4 — The regression tests (pre-registered)

- `test_per_regime_ic_weighting_defaults_off` — replaces the pinned-`True` test.
  Asserts the default is now `False`. **FAILS at pre-flip HEAD** (G1's before-half).
- `test_regime_ics_only_populated_under_ic_seed_weighting` — **new, and the
  load-bearing one.** Guards §2's conditional-inertness argument by asserting
  `update_regime_ics` has exactly one caller in `scripts/`, and that the call sits
  inside the `ic_seed_weighting`-gated block. If a second populate path is ever
  added, the flip's safety argument is void and this test says so.
- `test_no_arm_inherits_per_regime_ic_weighting` — **new.** Scans every
  feature-JSON arm in `scripts/*.sh` and asserts that any arm setting
  `ic_seed_weighting: true` also sets `per_regime_ic_weighting` explicitly. This is
  the generalisable half of V275 §8 item 2, applied to the one flag at hand.
- `test_ic_seed_weighting_defaults_off` and
  `test_ic_seed_weighting_has_no_strategy_layer_consumer` — retained unchanged.

---

## §5 — Gates

| Gate | Assertion | Status |
|---|---|---|
| **G1** (test discipline) | The new tests FAIL at pre-flip HEAD and PASS post-flip. Both directions captured verbatim. | §6 |
| **G2** (no behaviour change) | One representative cell per family, re-run at post-change HEAD via `ARMS=off bash scripts/v274_smoke.sh`, reproduces its committed V240 PnL **to the cent**: crisis `snap_wf_20240310` = **$1,149.76**, trend `snap_wf_20230912` = **$4,679.67**, recent `snap_wf_20250305` = **$771.98**. Any movement ⇒ **R5 STOP**. | §6 |
| **G2b** (the pin is a true no-op) | The IC-**ON** arm must also be unmoved by Part A. `ARMS=on bash scripts/v274_smoke.sh` at pre-change vs post-change HEAD must agree to the cent. This gate has no V275 analogue and is the one that actually tests the pin. | §6 |
| **G3** (arm audit) | All 7 IC-ON arms pinned; zero arms inherit the default afterwards. | §6 |
| **G-DET** | N=2 replicates per cell, spread **$0.00**, at the V240 condition (`sleep=0`, `cycles=60`). | §6 |

**Anti-Goodhart:** every gate here is a *reproduction* gate. There is no number V276
can win. The only outcomes are "unchanged" (ship) and "moved" (STOP).

**Substrate note:** G2/G2b require the frozen-cache preflight to pass. As of
`d93f7340` the manifest-blessed `data/macro_cache.db` (`397b943…`) is restored;
commit `cfb4a43d` had committed a drifted blob (`baeec866…`) that made every frozen
run abort at `run_training.py:641`, in every checkout **and** worktree.

---

## §6 — Results

_(to be filled in after the run — this section is empty at pre-registration time)_

### G1 — pending
### G2 / G2b / G-DET — pending
### G3 — pending

---

## §7 — Conclusion

_(pending)_

---

## §8 — Next steps for V277

_(pending — expected to carry forward V275 §8 item 2: promote the arm-inherit
scanner to a committed instrument reporting the inherit-rate of **every**
`VictoriaFeatures` flag across **every** arm.)_
