# V225 — additive crisis-skew signal (orthogonal crisis-predictive class)

**Date:** 2026-06-16
**Author:** claude
**Parent:** V224 (`15c871e`) — IC retired (3rd refutation). Incumbent is V221-era
equal-weight (IC-off). Standing #1 deficit is **crisis** (equal-weight crisis
−$3,621, negative in every version V170s→V224). Trend IC edge (+$2,206 hermetic)
banked as a future trend-only overlay, explicitly out of V225 scope.
**Status:** PRE-REGISTERED (2026-06-16). New-signal-class pivot.

## Hypothesis

The existing basket (sma, rsi, macd, bb, zscore, volume, funding) is
momentum/mean-reversion — all long-biased and all *coincident-to-lagging* in a
crash (they flip bearish only *after* price has already broken). The crisis gate
is negative in every version because nothing in the basket *leads* a drawdown.

**Bet:** an **additive, one-sided risk-off term** that fires *before* the basket
catches up — derived from realized **downside-semivariance skew** + **drawdown
acceleration** over the replay window — improves the **crisis** gate by > $200
**without regressing trend or recent by > $200**. It is additive (composite +
skew·W), NOT an IC re-weight (IC is closed — V224). Because it is one-sided
negative ([−1, 0]) and ≈ 0 in benign tape, the no-harm constraint on trend/recent
is satisfied *by construction* — the signal barely exists outside crisis.

**Effect-magnitude prediction:** crisis Δ between +$200 and +$3,000 (enough to
move the gate but bounded by W=0.5); trend/recent Δ within ±$200 (skew ≈ 0 there).

## Data-availability decision (the critical Plan question, pre-committed fork)

The V225 brief's **lead** (Deribit 25-delta options skew) and **fallback**
(cross-asset funding-spread) were both investigated against the actual snapshot
date ranges. **Both are unbuildable deterministically from data on disk:**

- **Deribit historical 25-delta IV skew — REJECTED.** Deribit
  `get_historical_volatility` is *realized* vol, not implied; the DVOL index is a
  vol *level*, not a 25-delta put/call *skew*, and has **no coverage for the
  2020-Q1 crisis window** (BTC DVOL post-dates it; ETH DVOL ~2022). Per-option
  historical IV/greeks for arbitrary 2020/2022 dates require a paid vendor
  (Tardis/Amberdata/Laevitas) — not free, not deterministic, not offline.
- **Cross-asset funding-spread — REJECTED as literally specified.** The snapshots
  hold a **single static funding scalar per symbol** (`_macro.funding_rates`, one
  venue, no time series, identical across 3 of 4 snapshots). There is **no
  per-venue axis to compute dispersion from** and no funding *history* to z-score;
  `FundingRateSignal`'s own V221 `max==min ⇒ 0.0` fence makes it inert in frozen
  mode. A real cross-venue spread would need a new vendor + a new frozen file —
  violating "funding data already wired, no new provider."

**Decision (no user check, per the pre-committed "default to the deterministic
fallback rather than asking" rule):** implement the **honest realization of the
fallback intent** — a crisis-predictive, orthogonal, risk-off signal built
**entirely from already-frozen snapshot OHLCV** (the only data axis that is both
already frozen *and* time-varying per cycle). This is `crisis_skew_signal`: a
**realized downside-semivariance skew**, which IS a skew (of the return
distribution) and IS crisis-leading, requiring zero new external feed. Honest
cost: it is a *realized* proxy (coincident-to-slightly-leading), weaker than true
*implied* skew. If it can't clear the falsifier, the V225 finding is "no free
crisis signal in spot-only data" → V226 escalates to a paid implied-skew vendor.

## Signal definition (`omega/nodes/victoria/signals/crisis_skew.py`)

Per (cycle, ticker), from the replay `close` window (length = window, ≥ 2):

```
r_i        = ln(close[i] / close[i-1])           # i = 1 … n-1, fixed order
downside   = fsum(r_i^2 for r_i < 0)             # downside semivariance
upside     = fsum(r_i^2 for r_i > 0)             # upside semivariance
skew_raw   = (downside - upside) / (downside + upside)   # ∈ [-1, 1]; 0 if denom==0
semivar    = max(0.0, skew_raw)                  # one-sided ∈ [0, 1]

peak       = max(close)                          # exact (no FP sum)
dd_now     = close[-1]/peak - 1                  # current drawdown ≤ 0
dd_prev    = close[-2]/peak - 1
accel      = clamp(0, 1, (dd_prev - dd_now) / 0.05)   # drawdown deepening this bar

risk_off   = clamp(0, 1, fsum([0.65*semivar, 0.35*accel]))
raw_value  = -risk_off                           # ∈ [-1, 0]  (risk-off ⇒ NEGATIVE)
```

- **Sign convention:** downside-skew steepening / drawdown accelerating ⇒
  `raw_value → -1` (risk-off / bearish brake). Benign tape ⇒ `raw_value → 0`.
  One-sided: never bullish (the basket already supplies the long side).
- **Lookback:** the replay window (30 bars). No rolling z-score (that reintroduces
  the length/order-sensitive accumulator the V220/V221 fences closed); the ratio
  construction is intrinsically bounded.
- **Missing data → 0.0, never NaN:** `len<2`, `max==min`, `denom==0`, `peak<=0`,
  non-finite ⇒ 0.0. Component C (static funding) dropped from the first cut
  (inert in frozen mode; candidate V226 add).

## Integration as additive composite member (NOT IC re-weight)

`signal_generation.py`, after the composite is set (after line 1041), gated on
`crisis_skew_enabled`:

```
skew = self._crisis_skew.compute(prices)        # stored under ts["crisis_skew"] (non-_signal key)
if skew != 0.0 and "composite" in ts:
    ts["composite"] = max(-1.0, min(1.0, math.fsum([ts["composite"], _SKEW_W * skew])))
```

- `_SKEW_W = 0.5`. **Post-composite additive** — NOT routed through the basket
  selector at `:1022` (key deliberately does NOT end in `_signal`), so the
  one-sided term is never trimmed away by `_balanced_composite`'s 20% trim (the
  Plan agent's #1 risk: a bottom-outlier crisis term would be trimmed exactly when
  it should bite). This is the faithful reading of "equal-weight basket **+ skew
  term**".
- Flag default **False** ⇒ live path byte-identical to the V224 equal-weight
  incumbent. Default-False also dodges the V224 control trap (where a `{}` control
  inherited `ic_seed_weighting=True`).

## Determinism plan

- Only new float site = the skew accumulator + the 2-element post-composite add,
  both `math.fsum` over fixed-order inputs. No `sum`/`np`/BLAS. No wall-clock, no
  network → `check_no_wallclock.py` green, hermetic by construction.
- **No new frozen file needed** — the signal reads only already-frozen snapshot
  OHLCV (Step 4 satisfied trivially; documented, no `frozen_options_skew.json`).
- Expectation: **4/4 hermetic at $0.00**. Any FAIL ⇒ the new fsum site opened a
  near-boundary selection; auto trade-diff (obs #14) names the field; fsum-fence
  or back out. Falsifier = "all cells PASS", never "the one I targeted."

## Observability shipped with V225

1. **`scripts/assert_cell_identity.py` (Step-1 item).** A preflight that reads a
   run's `results.json` and asserts its *actual* identity matches the cell label:
   `crisis_skew_enabled` == expected, `skew_on_cycles > 0` iff enabled (proves the
   term actually fired, not flag-on-but-inert), and `ic_on_cycles == 0` (genuine
   equal-weight, the exact V224 control bug — a cell claiming equal-weight while
   running IC on 200 cycles). Wired into `check_determinism.sh` (opt-in via
   `EXPECT_SKEW`/`EXPECT_IC` env), fires per replicate before the cell is accepted.
2. **`crisis_skew` observability block** in `results.json`: `crisis_skew_enabled`
   (bool) + `skew_on_cycles` (count of node-cycles where ≥1 ticker got a non-zero
   skew term). Plus a startup-banner line `crisis_skew: ON → ACTIVE | off`.

## Falsifier table (pre-committed fork — no moving goalposts)

Decisive comparison = **skew ON vs within-grid skew-OFF equal-weight control**
(same commit + caches), per gate. Both arms IC-off (`ic_seed_weighting:false`).
Anchor baselines (V224 within-grid equal-weight controls): trend −$1,330 (23t),
crisis −$3,621 (31t), recent +$4,747 (23t).

| Outcome | Reading |
|---|---|
| **Skew-ON beats equal-weight on crisis by > $200 AND trend/recent within ±$200** | **SUCCESS.** The orthogonal crisis brake works. V226 = tune `_SKEW_W` / stack with the banked trend-IC overlay. Update README crisis high-water if a record breaks. |
| **Crisis improvement < +$200** | Signal too weak (realized proxy lacks the lead of implied skew). V226 = escalate to a paid implied-skew vendor with the V215 freeze recipe, OR a different orthogonal class. |
| **Trend OR recent regresses > $200** | Signal is NOT orthogonal (it bites outside crisis) — harmful. Revert; re-tune the one-sidedness / lower `_SKEW_W`. |
| **Any 4-cell determinism FAIL** | The new fsum site opened a near-boundary channel. Auto trade-diff names the field; fence or back out before reading PnL. |

## Pre-committed risks + mitigations

- **Trim swallows a basket-routed one-sided term.** Mitigated by routing the skew
  **post-composite** (not as a `_signal` basket member) — it is never trimmed.
- **Realized ≠ implied skew** (coincident-leaning). Honest cost of the data
  verdict; the falsifier bar is unchanged. A miss is itself a publishable result.
- **W=0.5 too strong → trend/recent harm.** Mitigated by one-sidedness (skew ≈ 0
  in benign tape); if trend/recent still regress, the fork says revert + re-tune.
- **New fsum site → determinism channel.** Consumer is fsum-fenced; trade-diff
  self-names any residual.

## Gate results

| Gate    | PnL | Trades | WR | PF | Max DD | Notes |
|---------|----:|-------:|---:|---:|-------:|-------|
| recent  |     |        |    |    |        |       |
| trend   |     |        |    |    |        |       |
| crisis  |     |        |    |    |        |       |

Result files: `data/v225_<gate>_*`. Determinism grid: `data/v225_*_determinism/`.

## Conclusion

_(filled after the grid)_

## Next steps

→ **V226**: filled by whichever fork fires.
