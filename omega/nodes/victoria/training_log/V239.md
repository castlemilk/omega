# V239 — universe/blacklist flip on the V238 baseline (brief)

**Date pre-registered:** 2026-07-04 · **Author:** claude (Fable 5)
**Parent:** V238 (frozen-series feed build) · **Status:** queued brief —
full pre-registration to be written after V238's baseline lands.

## Hypothesis (one sentence)

The effective 4-name universe (ETH/ADA/NEAR/ARB) was a silent consequence of
`_TRADING_BLACKLIST` (V235 universe finding), never a measured decision;
flipping to the full 13-name universe on the V238 baseline (universe_full vs
universe_legacy cells, per the V235 universe-re-validation rule) measures
whether breadth adds cross-sectional spread — the thing a demeaned momentum
book fundamentally harvests — now that the information set is richer.

## Pre-committed structure

- Baseline: whichever distribution V238 adopts as standing (flag-ON if
  adopted, else the V235 main). The flip is measured on the FULL 32-window
  grid, universe as the only variable.
- Exclusion evidence bar (standing V235 rule): a ticker stays excluded only on
  hermetic-era N≥50 trades with a distributional read (≥3 windows).
- Universe changes are strategy changes: same acceptance shape as any
  mechanism — pre-registered mean/p25 bars per regime, written against V238's
  numbers when they exist. Noise threshold: recent 2·SE ≈ $2,400
  (REFLECTION_V237 §2) unless V238 re-estimates it.

## Queue behind it

V240 candidate: corr-spike portfolio tail cap (re-queued from the old V237
slot; retargeted falsifier in `V237.md`'s historical section remains its
execution spec; offline PCA/copula calibration is $0 and may run anytime).
Section-4 conditional-expectancy surface (DEEP_REVIEW §4) remains the parking
lot for all conditioning hypotheses, now to include V238's new series as axes.
