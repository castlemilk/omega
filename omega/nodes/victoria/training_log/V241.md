# V241 — reasoning-layer falsifier: does per-cycle basket review add expectancy?

## Results (2026-07-12, Fable 5) — **REFUTED. KEEP FLAG-GATED OFF.**

**Verdict: the reasoning layer is ACTIVE but adds variance, not expectancy.**
The pre-registered conjunction fails on both recent clauses; the layer stays
`reasoning_layer_enabled: false` and the standing baseline is unchanged
(V240 selective universe: crisis +$599 / trend +$2,997 / recent +$30).

### Falsifier clauses (pre-registered bars vs measured)

| clause | bar | measured | verdict |
|---|---|---:|---|
| recent mean-Δ | > +$400 | **+$226.56** | FAIL |
| recent p25-Δ | > +$500 | **−$705.44** | FAIL |
| trend mean-Δ | > −$300 | −$69.26 | pass |
| crisis mean-Δ | > −$300 | −$48.02 | pass |

### Phase 0 — inertness tracer (PASS → proceeded)

One recent sentinel fill first (`snap_wf_20250305`): 20 calls, 95%
intervention, 44 drops / 39 scale-downs over 108 candidate slots — neither
inert (<5%) nor veto-everything (>95% dropped). Full-fill pooled: **691 calls,
intervention rate 99.6%, drop fraction 42.6%**, p50 latency 15.2s, no window
below 5%. Tracer JSONL per window on gamma
(`v241_cache_fill/*_reasoning_trace.jsonl`), pooled report
`v241_cache_fill/intervention_report.json`.

### Contract deviation from the V240 draft (pre-declared at launch)

The V241 launch directive tightened the layer to drop/scale-**down** only:
`size_scale` clamp and prompt moved from [0, 1.5] → **[0, 1]** (never up-size
past the strategy's proposed weight). Committed 661704a before any fill call;
the whole cache was filled under the tightened prompt.

### Distributions (32 windows; Δ = reasoning_on − universe_selective)

| regime | n | baseline mean | ON mean | Δ mean | Δ p25 | Δ p50 | Δ p75 | Δ min | Δ max |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| crisis | 12 | +598.53 | +550.51 | −48.02 | −910.22 | +141.23 | +1,108.29 | −7,120.12 | +4,814.75 |
| trend | 10 | +2,996.92 | +2,927.65 | −69.26 | −938.04 | −99.05 | +4,178.21 | −18,733.44 | +8,611.71 |
| recent | 10 | +29.64 | +256.20 | +226.56 | −705.44 | +64.61 | +875.61 | −1,823.29 | +3,991.10 |

Pooled Δ: mean +$31.15, p25 −$910, spread **$27,345** (n=32).

**Reading:** the layer is a high-activity repricer that reshuffles PnL
violently window-to-window (single trend window `snap_wf_20201226` Δ
−$18,733; `snap_wf_20210624` Δ +$8,612) with a ~zero pooled mean. Recent's
+$227 mean is deep inside noise (per-window Δ sd $1,660 → 2·SE ≈ $1,050;
5/10 recent windows negative) and is carried by the single sentinel window
(+$3,991); the tail clause (p25 −$705 vs +$500) shows no coherent
tail improvement — precisely the failure mode the conjunction was designed
to catch. Trend/crisis means pass no-regression but with per-window swings
(±$7k–19k) far above their own baselines' dispersion: the layer trades
variance for nothing.

### Determinism / hermeticity (all PASS)

- OFF arm: NOT re-run — V240 confirm cells reused; 4-window reproducibility
  spot-check (one per regime + sentinel) **identical ex-run-timestamp**.
- Cache-fill under grid-identical frozen conditions via `OMEGA_LLM_CACHE_FILL=1`
  (LLM-only live; prompts hash-match replay — new override, see reasoning_layer.py).
- Frozen grid: 32 cells, **0 determinism FAILs**, 3 sentinels N=2 spread $0.00,
  100% cache hits, zero `LLMCacheMiss`.

### Provenance

- Code: 661704a (tracer + [0,1] cap + fill override), a88b95e (grid+aggregator),
  a1ed754 (frozen cache, 691 entries + MANIFEST md5s; agy 1.1.1,
  model string "Gemini 3.1 Pro (Low)", model_id gemini-3.1-pro-low).
- Artifacts (gamma): `v241/distribution.json`, `v241/grid_progress.log`,
  `v241_cache_fill/`, per-cell `v241wf_*_determinism/`;
  auto-generated table `V241_WALKFORWARD_RESULTS.md`.
- Fill wall-clock 4h16m (32 windows, 0 failures); grid 1h22m.

### Observability-gap audit

What would have caught this sooner? The tracer measured *activity*, not
*quality* — phase 0 could not distinguish "active and useful" from "active
and random." Deltas:

1. **Counterfactual drop scorer (S, ship with any reasoning revisit):** at
   fill time, join each window's tracer drops against the already-existing
   OFF-cell trade ledger and score the PnL of the trades the layer would have
   vetoed. The OFF cells predate the fill, so this verdict was computable
   ~4h before the grid ran. Queued in OBSERVABILITY-BACKLOG.md.
2. **Intervention→PnL ledger (M, queue):** per-window attribution of Δ to
   drops vs scale-downs (extend trade_field_diff.py join). Without it we
   cannot say whether the −$18.7k trend window came from a vetoed winner or
   a scaled loser.
3. **Call-count/latency predictor (S, queue):** fill cost is candidate-density
   driven (17–26 calls/window observed); a manifest-time estimate would have
   priced phase 1 exactly.

### Subsystem-patching audit

V236 (chop throttle, composite) → V237 (BTC-residualization, composite) →
V238 (frozen-series info layer) → V239/V240 (universe) → V241 (reasoning
layer) — five distinct subsystems, no patching loop. V241 was drawn from the
untouched-dimensions list (LLM basket review), evaluated once
distributionally, and refuted cleanly. Reflection: `REFLECTION_V241.md`
(required by the launch falsifier).

### Queue for V244+ (V242/V243 consumed by parallel Opus tracks)

- Corr-spike portfolio tail cap (spec V237.md, $0 offline calibration) — still
  the strongest untouched candidate.
- gdelt solo cell (source frozen V240.C, never evaluated).
- Reasoning-layer *veto-only, confidence-gated* variant — ONLY IF the
  counterfactual drop scorer (obs delta #1) first shows the drop set has
  negative-PnL skew on existing ledgers; do not re-grid on hope.
- {dxy, yield_curve} benign subset from V240.B.

## Update 2026-07-12 (V240 landed — bindings resolved, falsifier unchanged)

The pre-reg below stands with these substitutions, now that V240 shipped:

- **Model:** Gemini via `agy`, pinned `gemini-3.1-pro-low` (NOT Kimi — see
  `V240.md` "Model choice"; DeepSeek/Kimi/OpenRouter keys unavailable).
  Determinism comes from the frozen cache, certified in the V240.D smoke
  (frozen replay byte-identical; repeat-sampling agreement IDENTICAL).
- **Baseline binding:** the OFF arm = the **V240 selective-universe standing
  baseline** (`universe_selective_enabled: true`, crisis +$599 / trend
  +$2,997 / recent +$30) — reuse the V240 confirm-grid cells; OFF must be
  byte-identical to them.
- **Phase 0 added — inertness tracer (V213 lesson):** before the cache-fill
  pass, one recent-window fill; if the layer approves ~everything (no drops,
  no scales) or vetoes ~everything, that is a scaffolding/prompt bug, not an
  edge verdict — STOP and fix before burning the grid.
- **Cache-fill cost is wall-clock, not dollars:** ~16 s/agy call × ~1,800
  unique prompts ≈ 8h sequential fill ($0 API-side, agy is
  subscription-side). Fill runs flag-ON with `OMEGA_FROZEN_CACHE` unset;
  the grid then replays under `OMEGA_FROZEN_CACHE=1` only.
- **Queue seeded by V240 Track B** (not this version): regime-gated
  whale_flow (keep crisis +$2,063, drop trend −$2,433 tax); {dxy,
  yield_curve} benign subset (likely noise); gdelt solo cell (source frozen
  in V240.C); corr-spike tail cap (V242+, spec V237.md).

---

**Date pre-registered:** 2026-07-10 · **Author:** claude (Opus 4.8)
**Parent:** V240 (reasoning-layer scaffolding + smoke — must PASS first)
**Status:** pre-registered. Launches only after V240's scaffolding ships
(determinism certified, frozen cache filled and committed).

## Hypothesis (one sentence)

Turning `reasoning_layer_enabled` ON — the Kimi v2.7 per-cycle basket review
served from the committed frozen LLM cache — improves recent expectancy without
breaking trend/crisis, measured as a full 32-window walk-forward distribution
against the V240/V239 standing baseline.

## Pre-committed structure

- **Grid:** 32 windows × {reasoning OFF, reasoning ON} × N=1 sleep=0 = 64 cells
  on gamma, `OMEGA_AUDIT_OUTPUT_DIR` on gamma, reasoning served entirely from
  `data/frozen_llm_cache/` under `OMEGA_FROZEN_CACHE=1` (no live calls in the
  grid — the cache is pre-filled in V240's cache-fill pass and committed).
- **OFF cells must be byte-identical** to the V240/V239 standing baseline (the
  same OFF-identity contract as every version since V214).
- Every cell's determinism gate must PASS $0.00 (LLM-in-loop determinism was
  certified in V240; the grid re-certifies per cell).
- Baseline: whatever V239 adopts as standing (universe-flipped if adopted, else
  the V238 baseline).

## Falsifier (pre-registered — the version SHIPS ON only if ALL hold)

Reasoning ON adopted as standing **iff**, pooled over the walk-forward grid:

1. **recent mean-Δ > +$400** (the layer must earn its keep in the worst regime —
   recent is the standing target since V235), AND
2. **recent p25-Δ > +$500** (tail improvement, not just a mean pulled by one
   window), AND
3. **trend mean-Δ > −$300 AND crisis mean-Δ > −$300** (no regime regresses — the
   information/reasoning must not cost the regimes that already work).

Noise context: recent 2·SE ≈ $2,400 (REFLECTION_V237 §2) — a +$400 mean-Δ is
inside noise on its own, which is why the falsifier **conjoins** a tail bar
(p25) and no-regression bars rather than resting on the recent mean alone. If
the conjunction holds, the effect is coherent across mean+tail+other-regimes; if
any clause fails, the layer stays flag-gated OFF and per-cycle forensics decide
whether a narrower trigger (e.g. reasoning only in recent, or only as a veto
never an amplifier) is worth a follow-up.

## FAIL modes

- Determinism FAIL ⇒ bisect/fence per the standard arc before any verdict (an
  LLM cache-hit that is not byte-stable means the pin/temperature contract broke
  — fix in V240 territory, do not paper over in the grid).
- Cache miss under frozen mode ⇒ `LLMCacheMiss` aborts the cell; the cache-fill
  coverage was incomplete — re-fill and re-commit, never serve a neutral stub.

## Explicitly out of scope

Tuning the layer's keep/drop/size bounds or the retrieval index on the eval set
(that would be fitting the falsifier); live trading; any model other than the
V240-pinned Kimi v2.7.
