# V261 — On-chain flow primary universe (Track C) — offline scorer + walk-forward

**Date:** 2026-07-15 · **Author:** claude (Opus 4.8) · **Type:** Phase-0 offline scorer (pre-registration)
**Parent:** [`V256.md`](V256.md) (UNBLOCKED by V257) · [`V254_ALT_DATA_SCOPING.md`](V254_ALT_DATA_SCOPING.md) Track C
**Data:** [`V257_VERDICT.md`](V257_VERDICT.md) — 12 frozen Coin Metrics community series, BTC+ETH × 6 metrics, 2020-01→2026-07, byte-identical
**Precedent (discipline pattern):** [`V255_C.md`](V255_C.md) / `funding_carry/v255c_scorer.py`
**Code to be touched:** NEW module `omega/nodes/on_chain_flow/` only. **NO** edit to `strategy.py`,
`signal_generation.py`, `victoria_node.py`, `funding_carry/*`, or any flag. Parallel book (same rule as V255.C).

## Status: PRE-REGISTERED (this file committed BEFORE the scorer is built)

This is the V256 flow-primary offline test, run **exactly as V256 pre-registered** over whatever
signals V257 delivered (4 of 4, per-asset). No post-hoc redesign to fit the arrived data.

## Hypothesis (locked)

The four V256 pre-declared on-chain signals — net exchange netflow, active-address velocity,
whale-cluster movement (SplyExNtv proxy), transaction volume — composited into a per-asset z-score
signal produce a **directional** strategy on BTC + ETH spot with:

- Pooled median PnL > $0, bootstrap CI95 excludes zero
- Mann-Whitney U p < 0.05 on winners vs losers ranked by entry composite |z|
- Annualized gross return > 5%

OR a clean refutation with a specific reason.

## Pre-declared strategy — LOCKED parameter table (no post-hoc tuning)

| Parameter | Locked value |
|---|---|
| Universe | `BTCUSDT`, `ETHUSDT` (V257 on-chain coverage limit) |
| Price series (spot proxy) | Binance-futures **spot INDEX** price, `data/frozen_series/binance_futures/{SYM}/index_price.json` (the spot-index leg; cleaner spot proxy than mark — V255.D data path) |
| Component 1 — netflow | `FlowInExNtv − FlowOutExNtv` (native units/day, a **level**). Sign **−1×** (inflow = coins to exchanges = distribution/bearish) |
| Component 2 — active-address velocity | first difference Δ`AdrActCnt` (day-over-day). Sign **+1×** (rising network usage = bullish) |
| Component 3 — whale-cluster velocity | first difference Δ`SplyExNtv` (day-over-day exchange-held supply). Sign **−1×** (rising exchange supply = distribution/bearish) — **SplyExNtv PROXY, see caveat** |
| Component 4 — tx-volume velocity | first difference Δ`TxTfrCnt` (day-over-day transfers). Sign **+1×** (rising throughput = bullish) |
| Per-component z-score | `(x_t − mean_30) / std_30` over the **trailing 30 daily obs** (window inclusive of t), **sample** std (ddof=1) on the clean gapless on-chain grid |
| Degenerate guard | if `std_30 == 0` (or window < 30 obs) → component contributes **no** signal that day; if any component missing → no composite that day (V221 lesson: semantic-degeneracy fence, never an epsilon) |
| Composite z | `mean` of the four **signed** component z-scores (average keeps it on z-score scale) |
| Entry | `|composite z| ≥ 1.0` |
| Direction | `sign(composite z)` (+1 long, −1 short) |
| Sizing | fixed **$10,000** notional per trade |
| Hold | **5 days** (5 positions forward in the aligned price-date list; ≈5 calendar days on daily grid) |
| Position overlap | **non-overlapping per asset** — no new entry while in a trade (independent holds → annualize 365/5) |
| Fees | **5 bps maker per side** → 10 bps round-trip = fixed $10 per trade on $10k notional |
| PnL | `direction × (exit_price − entry_price)/entry_price × $10,000 − fees` |
| Span | full 2020-01→2026-07; trades only where entry + exit-5d prices both exist (price series starts 2020-06) |
| Bootstrap | 10,000 resamples, fixed seed 20250715, percentile CI95 on pooled median (deterministic, `random.Random`) |

## Falsifier (hard — any single clause → verdict cannot be ADOPT)

- **F1** Pooled median PnL ≤ $0 → **REFUTED**
- **F2** MWU p ≥ 0.05 on winners vs losers by entry composite |z| → mechanism did not hold
- **F3** Annualized gross < 5% → below noise floor
- **F4 (proxy honesty)** If the whale-cluster component (Δ`SplyExNtv`, the community proxy for true
  whale-cohort clustering) contributes ≥ 80% of the lift — i.e. removing it flips the verdict — the
  ADOPT is threatened by proxy risk → cap at KEEP-FLAG-GATED with the proxy caveat flagged.
- **Any post-hoc parameter change** to escape a fired falsifier → Goodhart, **abort** (record the
  refutation as-is; do not re-parameterize).

## Verdict mapping

- **ADOPT** (default OFF flag): F1–F3 all pass (median>0 & CI95 excl 0, MWU sig, ann gross ≥5%) AND
  F4 clean (whale proxy is not carrying ≥80% of the lift). Real alpha, flag-gated pending live wiring.
- **KEEP FLAG-GATED**: core alpha passes but marginal (CI touches 0) OR F4 proxy-dominance caveat fires.
- **REFUTED**: F1/F2/F3 fires — flow-primary lane closes with specific reason + numeric evidence.

**Cap:** no live on-chain feed exists yet, so the ceiling for this task is FLAG-GATED — live wiring is a
follow-on V###. This is the third parallel-alpha lane's verdict (spot Victoria + funding-carry BTC/ETH +
on-chain-flow BTC/ETH).

## Anti-Goodhart guard

The signal SET is whatever V257 froze (4/4). The parameter table above is locked in this file, committed
BEFORE any scorer code runs. If a falsifier fires, the refutation is recorded as-is; the response is NOT to
tune windows/weights/thresholds until it passes. SplyExNtv is a documented proxy (V257 §Proxy-note) —
the F4 clause exists specifically so a proxy-driven "win" cannot silently earn ADOPT.

## Integrity ledger (to be confirmed at verdict)

- New module `omega/nodes/on_chain_flow/` only. No strategy/flag edits. No live-broker. No network.
- Deterministic / byte-identical re-run. Artifacts under `/Volumes/gamma-systems-2/omega-victoria-data/v261/`.

## Gate results — **REFUTED** (see [`V261_VERDICT.md`](V261_VERDICT.md))

Full 4-component composite, 334 non-overlapping trades (BTC 171, ETH 163), 2020-06→2026-07, deterministic
(byte-identical re-run, md5 `d9d68239…`):

| Falsifier | Threshold | Measured | Fired? |
|---|---|---|---|
| F1 pooled median ≤ $0 | > $0 | +$11.74 (CI95 [−$45.69, +$62.69] spans zero) | no (but not sig) |
| **F2 MWU p ≥ 0.05** | p < 0.05 | **p = 0.942** (win \|z\| 1.275 ≈ lose 1.288) | **YES → REFUTED** |
| F3 annualized gross < 5% | ≥ 5% | 36.43% | no (magnitude w/o mechanism) |
| F4 SplyExNtv proxy dominance | not dominant | LOO-whale also fails core (p=0.62) | no (moot) |

**VERDICT: REFUTED.** The composite has no daily-bar dose-response on BTC/ETH — a stronger signal does not
predict a better outcome, and the positive pooled PnL is inside its own noise. Clean evidence, not data
absence. Track C closes. Params were locked & committed (`96d5d0e`) before the run; no post-hoc tuning.
